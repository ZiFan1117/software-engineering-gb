# 软件详细设计说明（LLD）

> **文档编号规则**：`<项目代号>-LLD-<顺序号>-v<主版本>.<次版本>`；按模块增量时可拆分为 `<项目代号>-LLD-<模块号>-v<主版本>.<次版本>`，示例 `<项目代号>-LLD-M20-v0.1`（规则见 §0.3）
> **适用阶段**：**S4 模块实现**（与每个模块的「详细设计 → 编码 → 单元测试」循环同步增量编写；**R4 模块评审**准出材料）；框架变更（**R5**）后同步回写
> **依据标准**：**GB/T 8567-2006**《计算机软件文档编制规范》（详细设计说明书文档种类）；需求追溯依据 GB/T 9385-2008；单元测试依据 **GB/T 38634.3-2020**；过程依据 GB/T 8566-2022
> **文档状态**：**模板（Template）** · 未基线化 · 禁止未经填写与评审直接作为交付物提交

---

## 零、使用说明（填完全文后请删除本章）

### 0.1 本模板的组织方式：**按模块增量编写**

详细设计**不是一次性写完的文档**，而是随 S4 的模块循环逐步生长的：

```
每个模块的循环（标准动作，不可跳步）：
① 读契约（03-模块接口契约.md）
      → ② 写本模块的详细设计章节（本文件新增一节）
      → ③ 设计单元测试（先设计用例，再写实现亦可）
      → ④ 编码
      → ⑤ 单元测试通过 → ⑥ 模块自测
      → ⑦ 框架适配性回判 ─┬─ 适配 → ⑧ 回写本文件（与代码同一次提交）
                           └─ 不适配 → 提 R5 框架变更 → 回到 S2 → 更新框架基线 → 重入 ④
```

**增量规则**

| 编号 | 规则 |
|---|---|
| IN-1 | 每个模块一节（§3.x），**节标题以模块号开头**，模块号与 `02-模块清单与模块号登记表.md` 一致 |
| IN-2 | 新增模块 = 新增一节；**不得**修改已完成模块的节号（只允许在其内部增补） |
| IN-3 | 每节必须填写「对应需求编号」，并与 RTM 的「设计模块号」列互相印证 |
| IN-4 | 代码变更若影响本节内容，**必须在同一次提交里更新本节**（文档与代码不得漂移） |
| IN-5 | 每节末尾必须有「单元测试设计对应表」，且用例编号与测试记录一致 |
| IN-6 | 本文件可拆分为每模块一份（`<项目代号>-LLD-<模块号>-vX.Y`），但**契约地址与编号规则不变** |

> **为什么不建议"设计全写完再编码"**：详细设计的价值在于**在编码前暴露算法与状态的坑**，
> 而不是产出厚文档。写一节、编一个模块、测一个模块，是最省返工的节奏。

### 0.2 与上游文档的关系

| 上游文档 | 本文件如何使用它 |
|---|---|
| `../02-需求类/01-软件需求规格说明.md` | 「对应需求编号」的来源；验收准则决定单元测试的判定口径 |
| `../02-需求类/02-接口需求规格说明.md` | 外部接口的字段表、错误码表以它为准，本文件只写**实现细节**（缓存、重试、序列化） |
| `01-软件概要设计说明.md` | 模块划分、分层、数据模型、错误码体系的来源 |
| `02-模块清单与模块号登记表.md` | 模块号的唯一来源 |
| `03-模块接口契约.md` | **本文件的上游契约**：契约定义"外部可见行为"，本文件定义"内部如何实现" |

> **边界**：本文件**不得**重新定义契约（入参/出参/错误码语义）。
> 若发现契约不足以表达实现需要 → 这是 `FC-1` 契约不足，**提 R5**，而不是在详细设计里"顺手扩展"。

### 0.3 编号规则

| 对象 | 编号格式 | 示例 |
|---|---|---|
| 本文档 | `<项目代号>-LLD-<顺序号>-v<主版本>.<次版本>` 或 `<项目代号>-LLD-<模块号>-vX.Y` | `PRJ-LLD-M20-v0.1` |
| 模块 | `M` + 2 位序号 | `<M01>` |
| 接口 | `IF-<3 位序号>` | `<IF-001>` |
| 需求 | `REQ-F-001` / `REQ-N-001` | `<REQ-F-001>` |
| 单元测试用例 | `TC-<3 位数字>`（`TC-0xx` 段） | `TC-001` |
| 集成/系统/验收用例 | `TC-1xx` / `TC-2xx` / `TC-3xx` | `TC-101` / `TC-201` / `TC-301` |
| 错误码 | `<域前缀>-<4 位数字>` | `AUTH-4011` |
| 状态机状态 | `S<2 位序号>` | `S01` |
| 待决事项 | `TBD-<2 位序号>` | `<TBD-01>` |
| 日期 / 人名 | `<YYYY-MM-DD>` / `<姓名>` | |

### 0.4 文档信息

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-LLD-001-v0.1` |
| 版本号 | v0.1 |
| 覆盖模块 | `<M20>`、`<M40>`（滚动增加） |
| 编写人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审人 / 日期 | `<姓名>` / `<YYYY-MM-DD>`（R4 模块评审，逐模块） |
| 关联文档 | HLD `<项目代号>-HLD-001`；模块接口契约 `<项目代号>-IC-001`；RTM `<项目代号>-RTM-001` |

### 0.5 修订记录

| 版本 | 日期 | 修订人 | 修订类型 | 覆盖模块 | 修订内容摘要 | 关联变更单 |
|---|---|---|---|---|---|---|
| v0.1 | `<YYYY-MM-DD>` | `<姓名>` | 新建 | `<M20>` | 认证服务详细设计 | — |
| v0.2 | `<YYYY-MM-DD>` | `<姓名>` | 增量 | `<M40>` | 查询仓储详细设计 | — |
| v0.3 | `<YYYY-MM-DD>` | `<姓名>` | 框架变更回写 | `<M20>`、`<M40>` | 契约扩展 priority 字段的实现调整 | `FC-<YYYY>-<NNN>` |

> **修订类型**：新建 / 增量 / 框架变更回写 / 缺陷修正。

---

## 1 引言

### 1.1 编写目的

说明每个模块**内部如何实现**：数据结构、算法、流程逻辑、状态机、错误处理、单元测试设计。
作为编码、单元测试、代码走查（R4）与后续维护的直接依据。

- **预期读者**：模块实现者、同级代码走查人、测试、维护者；
- **用途**：模块评审（R4）时逐项核对；维护阶段定位逻辑的第一入口。

### 1.2 背景

| 项 | 内容 |
|---|---|
| 产品名称 / 代号 | `<项目名称>` / `<项目代号>` |
| 框架基线 | `framework/v0.1`（`<项目代号>-HLD-001-v0.1`） |
| 需求基线 | `req-baseline/v1.0` |
| 本阶段在流程中的位置 | S4 模块实现（见 `docs/01-流程与阶段/阶段流程与交付物.md`） |

### 1.3 术语、定义与缩略语

| 术语 / 缩略语 | 全称 | 定义 |
|---|---|---|
| LLD | Low-Level Design | 软件详细设计说明 |
| 状态机 | State Machine | 模块内实体的状态与迁移规则集合 |
| 幂等 | Idempotent | 重复执行 N 次与执行 1 次效果相同 |
| 覆盖率 | Coverage | 行覆盖率 / 分支覆盖率；本仓库阈值：行 ≥ 80%，核心分支 ≥ 90% |
| `<术语>` | | |

### 1.4 参考资料

| 编号 | 名称 | 编号/版本 | 来源 |
|---|---|---|---|
| REF-01 | 软件概要设计说明 | `<项目代号>-HLD-001-v0.1` | 本项目 |
| REF-02 | 模块接口契约 | `<项目代号>-IC-001-v0.1` | 本项目 |
| REF-03 | 软件需求规格说明 | `<项目代号>-SRS-001-v1.0` | 本项目 |
| REF-04 | GB/T 8567-2006 计算机软件文档编制规范 | — | 国家标准 |
| REF-05 | GB/T 38634.3-2020 软件测试 第3部分：测试文档 | — | 国家标准 |

---

## 2 程序系统的结构

> 本节给出**模块到程序单元**的分解视图：一个模块号对应一个代码目录，模块内部的类/函数不另占模块号。

| 模块号 | 模块名称 | 代码目录 | 主要程序单元（类/文件） | 对外接口 | 对应需求 | 详细设计章节 | 状态 |
|---|---|---|---|---|---|---|---|
| `<M20>` | 认证服务 | `src/m20_auth/` | `service.py`（AuthService）、`lockout.py`（LockoutPolicy） | `<IF-001>`、`<IF-002>` | `<REQ-F-001>` | §3.1 | 已实现 |
| `<M40>` | 查询仓储 | `src/m40_query/` | `repo.py`（TaskRepo）、`cursor.py`（CursorCodec） | `<IF-003>` | `<REQ-N-001>` | §3.2 | 已实现 |

**结构检查**

| 检查项 | 判据 | 结论 |
|---|---|---|
| 模块号与代码目录一一对应 | 100% | ☐ |
| 依赖方向自上而下、无环 | 依赖图必须为 DAG | ☐ |
| 每个模块有独立可运行单元测试 | 100% | ☐ |
| 模块内部单元不跨模块直接引用 | 0 处 | ☐ |

---

## 3 模块详细设计（按模块增量编写）

> **复制 §3.1 的 15 个小节，为每个模块新增一节。**
> 节标题格式：`### 3.<n> <Mxx> <模块名称>`。

---

### 3.1 `<M20>` 认证服务

#### 3.1.1 模块描述

| 项 | 内容 |
|---|---|
| **模块号** | `<M20>` |
| **模块名称** | 认证服务 |
| **所属分层** | 业务层（`M20`–`M39`） |
| **契约版本** | IC-M20 `1.0.0`（见 `03-模块接口契约.md`） |
| **对应需求编号** | `<REQ-F-001>`（用户登录）、`<REQ-F-002>`（用户注销）、`<REQ-N-013>`（口令存储强度） |
| **实现语言 / 依赖** | `<语言><版本>`；依赖 `<M40>`、`<M60>`、`<M80>` |
| **代码位置** | `src/m20_auth/service.py` |
| **负责人** | `<姓名>` |
| **设计日期** | `<YYYY-MM-DD>` |
| **状态** | 设计中 / 已实现 / 已变更 |

**模块目标与非目标**

| 项 | 内容 |
|---|---|
| 目标 | 校验账号口令、维护失败计数与锁定、签发与撤销会话令牌 |
| **非目标** | 不做授权判定（`<M21>`）、不做口令强度策略（`<M22>`）、不直接访问数据库（经 `<M40>`）、不发通知（`<M70>`） |

#### 3.1.2 功能

| 功能编号 | 功能 | 触发 | 对应需求 | 契约接口 |
|---|---|---|---|---|
| F-01 | 账号口令认证 | 登录请求 | `<REQ-F-001>` | `<IF-001>` |
| F-02 | 失败计数与锁定 | 认证失败 | `<REQ-F-001>` | `<IF-001>` |
| F-03 | 会话令牌签发 | 认证成功 | `<REQ-F-001>` | `<IF-001>` |
| F-04 | 会话撤销 | 注销请求 | `<REQ-F-002>` | `<IF-002>` |

#### 3.1.3 性能

| 指标 | 要求 | 度量条件 | 关联需求 |
|---|---|---|---|
| 认证响应 | P95 ≤ 500ms（含下游 `<IF-005>`） | 50 并发，持续 10min | 由 SRS §4.3 性能需求分解到本模块 |
| 令牌校验 | P95 ≤ 10ms（本地校验，不查库） | — | 由 SRS §4.3 性能需求分解到本模块 |
| 并发 | 支持 200 并发登录，错误率 < 0.1% | 与生产同构环境 | `<REQ-N-002>` |
| 内存 | 无状态，常驻 ≤ 200MB | 单节点 | `<REQ-N-004>` 的模块级预算分解 |

> **性能实现要点**：令牌采用 JWT **本地验签**（不查库），是达成 P95 ≤ 10ms 的前提；
> 若改为服务端会话存储，需重估本条并提 R5。

#### 3.1.4 输入项

| 输入项 | 类型 | 必填 | 约束 | 校验时机 | 校验失败处置 |
|---|---|---|---|---|---|
| `account` | string | 是 | 6–32，`^[A-Za-z0-9_]+$` | 入口校验（先于任何 IO） | `AUTH-4001` |
| `password` | string | 是 | 8–64 | 入口校验 | `AUTH-4001` |
| `client_ip` | string | 否 | IPv4/IPv6 | 入口校验（格式不合法则忽略并记 WARN） | 忽略 |
| `captcha_token` | string | 连续失败 ≥ 3 次时是 | ≤ 256 | 读取计数后校验 | `AUTH-4001` |

#### 3.1.5 输出项

| 输出项 | 类型 | 产生条件 | 说明 |
|---|---|---|---|
| `token` | string | 认证成功 | JWT，HS256，含 `sub`/`exp`/`jti` |
| `expires_in` | integer | 认证成功 | 7200（秒），滑动续期 |
| `user.roles` | array[string] | 认证成功 | 可为空数组 |
| `code` / `message` / `trace_id` | string | 认证失败 | 错误码见契约 §6 |

#### 3.1.6 数据结构

| 结构名 | 类型 | 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|---|---|
| `AuthCommand` | 入参对象 | `account` | string | 见 §3.1.4 | |
| | | `password` | string | 8–64 | **不得**进入任何日志 |
| | | `client_ip` | string? | | |
| `UserRecord` | 持久化实体（`<M40>` 映射） | `user_id` | string | PK | |
| | | `account` | string | UK | |
| | | `password_hash` | string | bcrypt，cost ≥ 10 | `<REQ-N-013>` |
| | | `status` | enum | `ACTIVE`/`LOCKED`/`DISABLED` | |
| | | `fail_count` | int | ≥ 0 | 原子累加 |
| | | `lock_until` | datetime? | UTC | 为空表示未锁定 |
| `SessionToken` | 值对象 | `sub` | string | user_id | |
| | | `exp` | int | 签发时间 + 7200 | |
| | | `jti` | string | UUID | 用于撤销 |
| `LockoutState` | 领域状态 | `fail_count` | int | 0–5 | |
| | | `locked` | bool | | |

**存储与生命周期**

| 项 | 说明 |
|---|---|
| 持久化位置 | `t_user`（经 `<M40>`，**本模块不直接访问表**） |
| 缓存 | 无（认证路径不引入缓存，避免一致性问题） |
| 并发控制 | `fail_count` 使用数据库原子自增（`UPDATE ... SET fail_count = fail_count + 1`） |
| 敏感数据 | `password_hash` 只读不写；`password` 明文仅在内存中存在至校验结束，随即置空 |

#### 3.1.7 算法与流程

**算法 A-01：口令校验**

```
输入: account, password
输出: 成功 → SessionToken；失败 → 错误码
步骤:
  1. 入口格式校验（不合法 → AUTH-4001，不写失败计数）
  2. user ← M40.find_user_by_account(account)
  3. IF user 不存在:
       执行一次"假校验"（对固定假 hash 做 bcrypt 比较）
       → 使耗时与真实路径相当，防时序侧信道枚举账号
       → 返回 AUTH-4011（与口令错误同一错误码）
  4. IF user.status == DISABLED → AUTH-4011（不泄露状态）
  5. IF user.lock_until > now → AUTH-4231(retry_after = lock_until - now)
  6. ok ← bcrypt.verify(password, user.password_hash, cost ≥ 10)
  7. IF NOT ok:
       M40.increment_fail_count(user_id)   ← 原子操作
       IF 新计数 ≥ 5: M40.set_lock_until(user_id, now + 15min)
       写审计日志(WARN, account 掩码)
       返回 AUTH-4011 或 AUTH-4231（第 5 次）
  8. M40.reset_fail_count(user_id)
  9. token ← sign_jwt(sub=user_id, exp=now+7200, jti=uuid4())
 10. 写审计日志(INFO)，返回 token
```

**算法 A-02：令牌撤销**

```
输入: jti, user_id
步骤: 1. jti ← 加入撤销集合（TTL = 剩余有效期，避免集合无限增长）
      2. 幂等：重复撤销同一 jti 无副作用，返回成功
```

**主流程逻辑**

```
[登录请求]
    │
    ├─ 格式校验失败 ─────────────────────────→ AUTH-4001
    ▼
[查用户] ──不存在──→ [假校验] ──────────────→ AUTH-4011
    │
    ▼
[已锁定?] ──是──→ AUTH-4231 (+retry_after)
    │否
    ▼
[bcrypt 校验] ──失败──→ [计数+1] ──≥5──→ [锁定 15min] ─→ AUTH-4011/AUTH-4231
    │成功
    ▼
[清零计数] → [签发 JWT] → 200 {token, expires_in, user}
```

> **流程设计要点（必须在评审中说明理由）**
> 1. 格式校验**先于任何 IO**：避免恶意请求打穿到数据库。
> 2. 账号不存在时执行"假校验"：**使响应耗时不泄露账号是否存在**（安全需求 `<REQ-N-013>`）。
> 3. 失败计数用**原子自增**而非"读-改-写"：并发登录下不丢更新。

#### 3.1.8 状态机

> **适用性判定**：本模块存在"账号锁定"这一有明确状态与迁移条件的实体，**适用**。
> 若无状态实体，本节写「不适用」并说明理由（**不允许留空**）。

**实体：账号锁定状态（LockoutState）**

| 状态 | 含义 | 初始态 |
|---|---|---|
| `S01 正常` | `fail_count < 5`，`lock_until` 为空或已过期 | ✅ |
| `S02 锁定` | `lock_until > now` | |

**迁移表**

| 编号 | 起始态 | 事件 | 守卫条件 | 动作 | 目标态 | 副作用 |
|---|---|---|---|---|---|---|
| T-01 | S01 正常 | 认证失败 | `fail_count + 1 < 5` | `fail_count += 1` | S01 正常 | 审计日志 WARN |
| T-02 | S01 正常 | 认证失败 | `fail_count + 1 ≥ 5` | `fail_count += 1`；`lock_until = now + 15min` | S02 锁定 | 审计日志 WARN；锁定通知（可选） |
| T-03 | S01 正常 | 认证成功 | — | `fail_count = 0` | S01 正常 | 签发令牌 |
| T-04 | S02 锁定 | 认证尝试（任意） | `now < lock_until` | 不校验口令 | S02 锁定 | 返回 `AUTH-4231` + `retry_after` |
| T-05 | S02 锁定 | 时间推进 / 认证尝试 | `now ≥ lock_until` | `fail_count = 0`；`lock_until = null` | S01 正常 | 审计日志 INFO |
| T-06 | S02 锁定 | 管理员解锁 | 有权限 | `fail_count = 0`；`lock_until = null` | S01 正常 | 审计日志（**必记**操作人） |

**状态机检查**

| 检查项 | 判据 | 结论 |
|---|---|---|
| 每个状态可达 | 从 S01 可达全部状态 | ☐ |
| 每个状态有出口 | 不存在无法离开的"死状态" | ☐（S02 经 T-05/T-06 可离开） |
| 迁移条件互斥且完备 | 同一（状态, 事件）下的守卫条件互斥且覆盖全部情况 | ☐ |
| 迁移有测试覆盖 | 每条迁移至少 1 条单元测试 | ☐ 见 §3.1.12 |

#### 3.1.9 接口（实现视角）

> **契约的入参/出参/错误码语义以 `03-模块接口契约.md` 为准，本节只写实现细节。**

| 接口编号 | 调用点 | 调用方式 | 超时 | 重试 | 序列化 | Mock 方式 |
|---|---|---|---|---|---|---|
| `<IF-001>` | `AuthService.authenticate()` | 进程内 | — | — | — | 直接构造入参 |
| `<IF-005>` | `AuthAdapter.verify()` | HTTPS | 3s（读） | ≤ 1 次，间隔 500ms | JSON | 注入 `FakeAuthAdapter` |
| `<IF-003>` | `TaskRepo.*` | 进程内 | — | — | — | 注入内存仓储 |

#### 3.1.10 错误处理

| 错误码 | 触发点 | 处理方式 | 日志级别 | 是否记审计 | 用户可见信息 |
|---|---|---|---|---|---|
| `AUTH-4001` | 入口校验 | 直接返回，**不写失败计数** | WARN | 否 | 账号或口令格式不正确 |
| `AUTH-4011` | 用户不存在 / 口令错误 / 账号禁用 | 统一返回（不区分） | WARN | 是 | 账号或口令错误 |
| `AUTH-4231` | 锁定期间尝试 | 返回 `retry_after` | WARN | 是 | 账号已锁定，请 N 分钟后重试 |
| `AUTH-5001` | `<IF-005>` 超时/熔断 | 重试 1 次后返回 | ERROR | 是 | 服务暂时不可用 |
| `AUTH-5002` | 未预期异常 | 捕获 → 映射 → 记录完整堆栈 | ERROR | 是 | 系统繁忙，请提供 `trace_id` |

**错误处理通则**

| 项 | 约定 |
|---|---|
| 禁止泄露 | 错误信息不得包含：账号是否存在、内部堆栈、SQL、下游地址 |
| 敏感字段 | `password` 在任何日志/异常/追踪中必须掩码为 `******` |
| 兜底 | 所有未映射异常统一转 `AUTH-5002`，堆栈只进日志 |
| 幂等重试 | 仅 `AUTH-5001` 允许重试 ≤ 1 次；其余不重试 |

#### 3.1.11 限制条件与注释设计

| 编号 | 限制条件 | 影响 | 规避方式 |
|---|---|---|---|
| L-01 | bcrypt cost ≥ 10，单次校验约 50–100ms | 高并发下 CPU 密集 | 并发上限与限流；cost 不得为性能而调低（`<REQ-N-013>` 红线） |
| L-02 | JWT 本地验签，无法即时失效 | 注销后令牌在剩余有效期内仍可验签通过 | 撤销集合（`jti` 黑名单）+ TTL = 剩余有效期 |
| L-03 | 依赖 `<M40>` 的原子自增能力 | 若仓储不支持原子操作，锁定策略失效 | 契约中要求原子自增；无则提 `FC-1` |
| L-04 | 时钟依赖 | 锁定判定依赖系统时钟 | 统一 UTC；NTP 同步；允许 ±1s 偏差 |

| 注释要求 | 说明 |
|---|---|
| 必须注释 | 算法 A-01 第 3 步（假校验）的理由——**这是安全设计，不注释会被后人"优化"掉** |
| 必须注释 | 锁定阈值 5 次 / 15 分钟的来源（`<REQ-F-001>`） |
| 禁止注释 | 复述代码本身（`i += 1  # i 加一`） |

#### 3.1.12 单元测试设计对应表

> **R4 准出**：单元测试全部通过；行覆盖率 ≥ 80%，核心分支 ≥ 90%；覆盖正常、边界、异常、空值四类路径。
> 用例编号规则：`TC-<3 位数字>`，**全局唯一、永不复用**；单元测试落在 `TC-0xx` 段（见 `../02-需求类/03-需求追溯矩阵.说明.md` §2.0.1）。
> 本表编号与 `../02-需求类/03-需求追溯矩阵.csv` 的「单元测试用例」列**逐字符一致**。

| 用例编号 | 测试对象 | 覆盖功能/迁移 | 对应需求 | 输入/条件 | 期望结果 | 路径类型 | 状态 |
|---|---|---|---|---|---|---|---|
| `TC-001` | `service.authenticate` | F-01 | `<REQ-F-001>` | 合法入参 + 正确口令 | 200，返回有效 JWT | 正常 | ✅ 通过 |
| `TC-002` | `service.authenticate` | F-01 | `<REQ-F-001>` | `account` 长度 5（下界-1） | `AUTH-4001`，失败计数不变 | 边界 | ✅ 通过 |
| `TC-004` | `service.authenticate` | F-01 | `<REQ-F-001>` | 正确账号 + 错误口令 | `AUTH-4011` | 异常 | ✅ 通过 |
| `TC-005` | `service.authenticate` | F-02 / T-02 | `<REQ-F-001>` | 连续第 5 次失败 | `AUTH-4231`，`lock_until = now+15min` | 边界 | ✅ 通过 |
| `TC-006` | `service.authenticate` | T-04 | `<REQ-F-001>` | 锁定期间正确口令 | `AUTH-4231`，不校验口令 | 异常 | ✅ 通过 |
| `TC-007` | `service.authenticate` | T-05 | `<REQ-F-001>` | 锁定已过期 | 恢复成功登录 | 边界 | ✅ 通过 |
| `TC-008` | `service.authenticate` | F-01 | `<REQ-F-001>` | 账号不存在 | `AUTH-4011`，且耗时与真实路径差异 ≤ 20% | 异常 | ✅ 通过 |
| `TC-009` | `service.authenticate` | F-01 | `<REQ-F-001>` | `password = ""` / `null` | `AUTH-4001` | 空值 | ✅ 通过 |
| `TC-010` | `AuthAdapter.verify` | 接口异常 | `<REQ-F-001>` | `<IF-005>` 注入超时 | 重试 1 次；仍失败 → `AUTH-5001`；计数不变 | 异常 | ✅ 通过 |
| `TC-011` | `lockout` 并发 | F-02 并发 | `<REQ-F-001>` | 10 线程并发失败 | `fail_count` 精确为 10（无丢更新） | 并发 | ✅ 通过 |
| `TC-012` | `revoke` | F-04 | `<REQ-F-002>` | 重复撤销同一 `jti` | 两次均成功，无副作用 | 幂等 | ✅ 通过 |
| `TC-013` | 日志脱敏 | 安全 | `<REQ-N-013>` | 任意登录请求 | 日志中无明文口令 | 安全 | ✅ 通过 |

> **编号跳号说明**：`TC-003` 已被 `<M40>` 的查询执行计划用例占用，故本模块从 `TC-004` 继续。
> **编号是全局资源，不按模块分段**——这是"编号永不复用"在跨模块场景下的必然结果。

**覆盖率与门禁**

| 指标 | 阈值 | 实测 | 结论 |
|---|---|---|---|
| 行覆盖率 | ≥ 80% | `<n>%` | ☐ |
| 核心分支覆盖率 | ≥ 90%（`authenticate` 全部分支） | `<n>%` | ☐ |
| 四类路径覆盖 | 正常/边界/异常/空值 均 ≥ 1 条 | — | ☐ |
| 状态机迁移覆盖 | T-01…T-06 全覆盖 | — | ☐ |

#### 3.1.13 尚未解决的问题

| 编号 | 问题 | 影响 | 责任人 | 计划解决时点 | 状态 |
|---|---|---|---|---|---|
| `<TBD-01>` | 锁定阈值取 5 次还是 3 次，待甲方确认 | 影响 `TC-005` 判定口径 | `<姓名>` | R4 前 | ⏳ 未关闭 |

---

### 3.2 `<M40>` 查询仓储（第二个模块从本节继续复制）

> 按 §3.1 的 13 个小节逐项填写，模块号替换为 `<M40>`。
> **查询类模块的「状态机」通常不适用**，此时写「不适用」并说明理由（如：本模块为无状态查询，无状态实体）。

---

## 4 引用文件与配置项清单

| 编号 | 文件/配置项 | 路径 | 版本 | 说明 |
|---|---|---|---|---|
| F-01 | 源码根目录 | `src/` | — | 模块号与目录一一对应 |
| F-02 | 单元测试根目录 | `tests/` | — | 用例编号 `TC-0xx`（与 RTM 的「单元测试用例」列一致） |
| F-03 | 覆盖率配置 | `<路径>` | — | 阈值：行 ≥ 80%，核心分支 ≥ 90% |
| F-04 | 契约文件 | `templates/03-设计类/03-模块接口契约.md` | `<版本>` | 契约的唯一权威来源 |

---

## 5 词汇表

| 词汇 | 说明 |
|---|---|
| `<术语>` | |

---

## 6 填写指引

1. **最常见错误：一次性把详细设计全写完再编码。** 详细设计的价值是"在编码前暴露算法与状态的坑"。**写一节、编一个模块、测一个模块**，是最省返工的节奏；写完再编只会让文档变成事后追认。
2. **最常见错误：状态机一节留空。** 必须显式回答"适用 / 不适用"。适用时要做可达性、出口与迁移互斥性检查；**每条迁移至少一条单元测试**。不适用时写明理由，不允许空白。
3. **最常见错误：把契约内容抄进详细设计并"顺手扩展"。** 入参/出参/错误码语义以 `03-模块接口契约.md` 为准。契约表达不了实现需要 = `FC-1` 契约不足，**提 R5**，不是在详细设计里私改。
4. **最常见错误：只设计正常路径的单元测试。** 每个模块必须覆盖**正常、边界、异常、空值**四类路径（R4 门禁项），阈值行 ≥ 80%、核心分支 ≥ 90%。只测 happy path 是缺陷重灾区。
5. **最常见错误：改了代码不回写本节。** 代码变更若影响详细设计，**必须在同一次提交里更新本节**（IN-4）；框架变更（R5）后必须回写受影响模块的详细设计（修订类型 `框架变更回写`）。
6. **最常见错误：关键设计决策不写理由。** 例如"账号不存在时做假校验""失败计数用原子自增"——这类反直觉的设计**必须注释理由**，否则会被后人当作冗余代码"优化"掉，直接引入安全漏洞。

---

## 7 依据说明

- 本文档的**章节骨架**（引言 / 编写目的 / 背景 / 定义 / 参考资料 / 程序系统的结构 / 程序（模块）设计说明 / 模块描述 / 功能 / 性能 / 输入项 / 输出项 / 算法 / 流程逻辑 / 接口 / 存储分配 / 注释设计 / 限制条件 / 测试计划 / 尚未解决的问题）按 **GB/T 8567-2006《计算机软件文档编制规范》** 对**详细设计说明书**这一文档种类的要求组织；其中「状态机」「数据结构」「错误处理」「单元测试设计对应表」为本仓库按通行工程结构归并的等价小节；单元测试的用例与记录要求参照 **GB/T 38634.3-2020**；需求追溯参照 GB/T 9385-2008。
- **保守处理声明**：GB/T 8567-2006 中详细设计说明书的精确分节标题、层级与条款编号，本模板未逐字复制（国标正文有版权，且本仓库不复制标准正文）。§0.1 的按模块增量编写规则（IN-1…IN-6）、§3.1.8 状态机的检查项、§3.1.12 单元测试设计对应表与覆盖率阈值均为**本仓库的工程强化项**，不是标准原文要求；其中 `TC-\d{3}` 用例编号与 `TC-0xx/1xx/2xx/3xx` 级别编码沿用本仓库门禁工具 `tools/trace_matrix.py` 的既有约定。**章节骨架按 GB/T 8567-2006 对详细设计说明书文档种类的要求组织，实施时请以标准正文为准。**
- 标准状态核验日期：2026-02；使用前请到[国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/gb/)复核现行性。
