# 软件概要设计说明（HLD）

> **文档编号规则**：`<项目代号>-HLD-<顺序号>-v<主版本>.<次版本>`，示例 `<项目代号>-HLD-001-v0.1`（规则见 §0.2）
> **适用阶段**：**S2 框架设计**（准出门禁：**R2 框架评审**通过 + **框架基线** `framework/v0.1` 已打标签）；S4 阶段随框架变更（**R5**）增量维护
> **依据标准**：**GB/T 8567-2006**《计算机软件文档编制规范》（概要设计说明书文档种类）；需求追溯依据 GB/T 9385-2008；过程依据 GB/T 8566-2022
> **文档状态**：**模板（Template）** · 未基线化 · 禁止未经填写与评审直接作为交付物提交

---

## 零、使用说明（填完全文后请删除本章）

### 0.1 本模板的立场：S2 的框架是**假设**，不是**真理**

概要设计不是"一次设计到位"的成品，而是**第一版可被证伪的假设集**。
因此本模板的准出门禁不看"设计得多完整"，而看三件事：

1. **边界清晰**：模块划分高内聚、低耦合、**无循环依赖**；
2. **契约可执行**：每个模块有明确职责与对外契约（`03-模块接口契约.md`）；
3. **假设显式**：**框架假设清单已成文**，每条假设都有**验证时点**（见 §7）。

> **关键心态**：实现阶段推翻框架**不是失败**，前提是——假设当初被写下来了。
> 没有写下来的假设被推翻，叫返工；写下来的假设被推翻，叫**受控的迭代**（触发条件 FC-3）。

### 0.2 编号规则

| 对象 | 编号格式 | 示例 | 说明 |
|---|---|---|---|
| 本文档 | `<项目代号>-HLD-<顺序号>-v<主版本>.<次版本>` | `PRJ-HLD-001-v0.1` | 框架版本从 **v0** 起，**不要过早宣布 v1.0** |
| 设计模块 | `M<2 位序号>` | `<M01>` | 规则见 `02-模块清单与模块号登记表.md`，**永不复用** |
| 接口 | `IF-<3 位序号>` | `<IF-001>` | 与 `../02-需求类/02-接口需求规格说明.md` 编号一一对应 |
| 框架假设 | `A-<2 位序号>` | `A-01` | 与 `docs/01-流程与阶段/框架与模块共演化.md` §六 格式一致 |
| 框架变更 | `FC-<YYYY>-<3 位序号>` | `FC-2026-007` | 触发条件 FC-1…FC-6 |
| 需求 | `REQ-F-001` / `REQ-N-001` | `<REQ-F-001>` | 与 SRS 编号逐字符一致 |
| 待决事项 | `TBD-<2 位序号>` | `<TBD-01>` | 必须写明责任人与关闭日期 |
| 日期 / 人名 | `<YYYY-MM-DD>` / `<姓名>` | | 不使用相对时间 |

### 0.3 文档信息

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-HLD-001-v0.1` |
| 版本号 | v0.1（框架基线 `framework/v0.1`） |
| 编写人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审人 / 日期 | `<姓名>` / `<YYYY-MM-DD>`（R2 框架评审） |
| 批准人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 关联文档 | 软件需求规格说明 `<项目代号>-SRS-001`；模块登记表 `<项目代号>-MODREG-001`；模块接口契约 `<项目代号>-IC-001`；需求追溯矩阵 `<项目代号>-RTM-001` |

### 0.4 修订记录

| 框架版本 | 日期 | 修订人 | 修订类型 | 修订内容摘要 | 关联变更单 | 基线标签 |
|---|---|---|---|---|---|---|
| v0.1 | `<YYYY-MM-DD>` | `<姓名>` | 新建 | S2 首次框架基线 | — | `framework/v0.1` |
| v0.2 | `<YYYY-MM-DD>` | `<姓名>` | 框架变更 | 例如：扩展 `<IF-003>` 契约增加 priority 字段 | `FC-<YYYY>-<NNN>` | `framework/v0.2` |
| | | | | | | |

> **修订类型**：新建 / 框架变更 / 基线冻结。
> **规则**：任何框架变更（R5 批准）必须在**同一次提交**里同步修改本文件与 `03-模块接口契约.md`——**文档与代码不得漂移**。

---

## 1 引言

### 1.1 编写目的

说明 `<项目名称>` 的**总体结构、模块划分、接口、数据与运行设计**，作为模块详细设计（S4）、骨架搭建（S3）、集成测试（S5）的共同基准。

- **预期读者**：架构师、开发、测试、运维、质量；
- **用途**：框架基线化后，任何结构或契约变更必须走 **R5 框架变更评审**，并同步更新本文档、模块契约与需求追溯矩阵。

### 1.2 背景

| 项 | 内容 |
|---|---|
| 产品名称 / 代号 | `<项目名称>` / `<项目代号>` |
| 任务来源 | 合同 / 任务书 / 内部立项单 `<编号>` |
| 需求基线 | `req-baseline/v1.0`（`<项目代号>-SRS-001-v1.0`） |
| 本阶段在流程中的位置 | S2 框架设计（见 `docs/01-流程与阶段/阶段流程与交付物.md`） |

### 1.3 术语、定义与缩略语

| 术语 / 缩略语 | 全称 | 定义 |
|---|---|---|
| HLD | High-Level Design | 软件概要设计说明 |
| LLD | Low-Level Design | 软件详细设计说明 |
| 契约 | Contract | 框架与模块之间的唯一法定接口，见 `03-模块接口契约.md` |
| 分层 | Layering | 表现层 / 业务层 / 数据访问层 / 基础设施层 / 公共工具层 |
| 循环依赖 | Cyclic Dependency | 模块 A→B→A 的依赖环，**R2 门禁红线，一票否决** |
| 框架假设 | Framework Assumption | S2 阶段对运行环境/数据/调用方式的未验证前提 |
| `<术语>` | | |

### 1.4 参考资料

| 编号 | 名称 | 编号/版本 | 来源 |
|---|---|---|---|
| REF-01 | `<项目名称>` 软件需求规格说明 | `<项目代号>-SRS-001-v1.0` | 本项目 |
| REF-02 | `<项目名称>` 接口需求规格说明 | `<项目代号>-IRS-001-v1.0` | 本项目 |
| REF-03 | GB/T 8567-2006 计算机软件文档编制规范 | — | 国家标准 |
| REF-04 | 框架与模块共演化 | `docs/01-流程与阶段/框架与模块共演化.md` | 本仓库 |
| REF-05 | `<技术栈>` 官方架构指南 | `<版本>` | 官方 |

---

## 2 总体设计

### 2.1 需求规定

说明本设计覆盖的需求范围与映射关系（**正向追溯的起点**）。

| 项 | 内容 |
|---|---|
| 覆盖需求 | `<REQ-F-001>` – `<REQ-F-0nn>`；`<REQ-N-001>` – `<REQ-N-0nn>` |
| 覆盖接口 | `<IF-001>` – `<IF-0nn>` |
| 需求覆盖率 | 100%（每条第 4 章体系结构中的模块均能回溯到需求；未覆盖项须在 §2.7 说明） |
| **暂不覆盖的需求** | 例如：`<REQ-F-045>`（P2，计划 v2 实现），理由与批准人：`<姓名>` |

### 2.2 运行环境

| 层 | 组件 | 版本 / 规格 | 说明 |
|---|---|---|---|
| 客户端 | 浏览器 / 客户端 | `<版本要求>` | |
| 应用服务器 | `<OS>` / `<运行时>` | 4C8G × 2 | 应用节点 |
| 数据库 | `<产品>` | 8C32G，主备 | |
| 中间件 | MQ / 缓存 | `<产品版本>` | |
| 网络 | 内网 / 专线 | 带宽 `<N>` Mbps | |

### 2.3 基本设计概念和处理流程

说明架构风格选型与**选型理由**（R2 明确要求"理由，不是'因为流行'"）。

| 项 | 内容 |
|---|---|
| 架构风格 | 例如：分层单体 / 分层 + 事件驱动 / 微服务 |
| **选型理由** | 例如：团队 4 人、无专职运维 → 微服务的运维成本不可承受；业务事务强一致 → 优先单体 + 清晰分层 |
| 被否决的方案 | 例如：微服务（理由：运维成本、分布式事务复杂度）——**必须写出被否决的方案，否则选型无法被评审** |
| 核心处理流程 | 用文字或 ASCII 图给出主流程（3–5 步以内） |
| 关键设计原则 | 例如：依赖单向向下；跨层调用禁止；模块间只经契约通信 |

**主流程示意**

```
[用户/外部系统] → 表现层(M01–M19) → 业务层(M20–M39) → 数据访问层(M40–M59)
                                        ↓
                              基础设施层(M60–M79)
                                        ↓
                              公共/工具层(M80–M99)
```

### 2.4 体系结构（结构）

#### 2.4.1 分层与模块编号分带

| 分层 | 模块号区间 | 职责 | 允许依赖 |
|---|---|---|---|
| 表现层 | `M01`–`M19` | 界面、输入校验、视图组装 | 业务层、公共/工具层 |
| 业务层 | `M20`–`M39` | 业务规则、用例编排、事务边界 | 数据访问层、基础设施层、公共/工具层 |
| 数据访问层 | `M40`–`M59` | 持久化、查询、外部数据源适配 | 基础设施层、公共/工具层 |
| 基础设施层 | `M60`–`M79` | 日志、配置、认证、MQ、缓存等技术能力 | 公共/工具层 |
| 公共/工具层 | `M80`–`M99` | 无业务语义的通用能力（日期、字符串、校验） | **不得依赖任何其他层** |

> **依赖规则铁律**：**只允许上层依赖下层，禁止反向依赖，禁止跨层跳跃（表现层不得直连数据访问层）。**
> 完整编号规则与登记表见 `02-模块清单与模块号登记表.md`。

#### 2.4.2 模块依赖图

```
        ┌──────────────┐        ┌──────────────┐
        │   M01 表现层  │        │   M02 表现层  │
        └──────┬───────┘        └──────┬───────┘
               │                       │
               └───────────┬───────────┘
                           ▼
                   ┌───────────────┐
                   │  M20 业务层    │
                   └───┬───────┬───┘
                       │       │
          ┌────────────▼─┐   ┌─▼──────────────┐
          │ M40 数据访问  │   │ M60 基础设施    │
          └──────────────┘   └────────────────┘
                       │       │
                       └───┬───┘
                           ▼
                   ┌───────────────┐
                   │ M80 公共/工具  │
                   └───────────────┘
```

| 检查项 | 判据 | 结论 |
|---|---|---|
| 是否存在循环依赖 | 依赖图必须为有向无环图（DAG） | ☐ 无环 |
| 是否存在跨层跳跃 | 表现层 → 数据访问层 直连数 = 0 | ☐ 0 |
| 是否存在反向依赖 | 下层调用上层次数 = 0 | ☐ 0 |
| 校验方式 | `python skeleton/run_skeleton.py` 的依赖拓扑检查 / CI 门禁 | ☐ 通过 |

#### 2.4.3 模块清单（摘要）

> 完整登记表见 `02-模块清单与模块号登记表.md`；此处只列本次基线包含的模块。

| 模块号 | 模块名称 | 所属分层 | 一句话职责 | 对外提供接口 | 关联需求 |
|---|---|---|---|---|---|
| `<M01>` | `<模块名称>` | 表现层 | | `<IF-010>` | `<REQ-F-001>` |
| `<M20>` | 认证服务 | 业务层 | 账号口令认证与会话管理 | `<IF-001>` | `<REQ-F-001>` |
| `<M40>` | 查询仓储 | 数据访问层 | 任务列表查询与分页 | `<IF-003>` | `<REQ-N-001>` |

#### 2.4.4 框架—模块共演化接口

本设计明确标注**哪些是框架能力、哪些是模块职责**，以避免"框架万能化"（反模式）。

| 项 | 内容 |
|---|---|
| 框架提供的公共能力 | 模块注册与依赖解析、配置加载、日志、错误码体系、契约校验、Mock 装配、统一鉴权 |
| 模块必须自行实现 | 业务规则、数据校验、领域状态流转、模块级异常映射 |
| 骨架（S3）的形态 | 各模块以桩/Mock 实现装配，主流程贯通，一条命令可跑通（`python skeleton/run_skeleton.py`） |
| 框架变更入口 | 命中 `FC-1`…`FC-5` 任一条 → 提 R5 框架变更评审（见 `docs/01-流程与阶段/框架与模块共演化.md` §三） |

### 2.5 功能需求与模块的关系

> **R2 准出项**：模块清单必须完整覆盖全部已基线需求（RTM 正向追溯无缺口）。

| 需求编号 | 需求名称 | 承载模块 | 设计章节 | 接口编号 | 备注 |
|---|---|---|---|---|---|
| `<REQ-F-001>` | 用户登录 | `<M20>`（`<M60>` 适配） | §3.3 | `<IF-001>` | |
| `<REQ-F-002>` | 用户注销 | `<M20>` | §3.3 | `<IF-002>` | |
| `<REQ-N-001>` | 列表查询性能 | `<M40>`、`<M20>` | §5.2 | `<IF-003>` | 性能约束见 §4.3 |

### 2.6 人工处理过程

| 编号 | 人工环节 | 触发条件 | 操作角色 | 系统支持 | 失败处置 |
|---|---|---|---|---|---|
| HP-01 | 例如：异常数据人工复核 | 校验失败且重试 3 次 | 业务管理员 | 复核界面 + 工单 | 转人工工单流程 |
| HP-02 | 例如：生产发布审批 | 每次发布 | 技术负责人 | 发布评审单 R8 | 驳回并记录 |

### 2.7 尚未解决的问题（设计遗留）

| 编号 | 问题 | 影响范围 | 责任人 | 计划解决时点 | 状态 |
|---|---|---|---|---|---|
| `<TBD-01>` | 例如：跨租户数据隔离是走行级过滤还是物理分库，待压测数据支撑 | `<M40>`、`<M20>` | `<姓名>` | `framework/v0.3` 前 | ⏳ 未关闭 |

---

## 3 接口设计

### 3.1 用户接口

| 编号 | 界面/入口 | 使用者角色 | 关键交互 | 输入校验规则 | 关联需求 |
|---|---|---|---|---|---|
| UI-01 | 登录页 | 全部 | 账号 + 口令 + 登录 | 见 `<REQ-F-001>` 输入定义 | `<REQ-F-001>` |
| UI-02 | `<界面名称>` | | | | |

**用户接口设计约定**

| 项 | 约定 |
|---|---|
| 交互一致性 | 同一操作在不同页面的交互方式必须一致；破坏性操作必须二次确认 |
| 操作步数 | 主流程操作步数 ≤ 5 步（可验证指标，见 `<REQ-N-011>`） |
| 错误提示 | 提示面向用户、不含堆栈；必须携带 `trace_id` 便于报障 |
| 无障碍 | 键盘可达；关键控件有语义标签 |

### 3.2 内部接口

模块之间的接口，**唯一法定形态是 `03-模块接口契约.md`**。此处登记索引。

| 接口编号 | 接口名称 | 提供方 | 消费方 | 调用方式 | 契约章节 | 关联需求 |
|---|---|---|---|---|---|---|
| `<IF-003>` | 任务仓储查询 | `<M40>` | `<M20>` | 进程内调用 | IC §M40 | `<REQ-N-001>` |
| `<IF-004>` | 登录页提交 | `<M01>` | `<M20>` | 进程内调用 | IC §M20 | `<REQ-F-001>` |
| `<IF-005>` | 认证服务适配 | `<M60>` | `<M20>` | 进程内调用 | IC §M60 | `<REQ-F-001>` |

### 3.3 外部接口

| 接口编号 | 接口名称 | 对端系统 | 协议 / 数据格式 | 同步异步 | 鉴权 | 详见 |
|---|---|---|---|---|---|---|
| `<IF-001>` | 统一认证登录 | `<外部系统 A>` | HTTPS/REST · JSON | 同步 | 双向 TLS + 应用凭证 | IRS §3.2.1 |
| `<IF-002>` | 用户注销 | `<外部系统 A>` | HTTPS/REST · JSON | 同步 | 双向 TLS + 应用凭证 | IRS §3.2.1 |
| `<IF-007>` | 任务状态变更通知 | `<外部系统 C>` | MQ · JSON | 异步 | 内网 + 消息签名 | IRS §3.2.2 |

> **外部接口的字段表、错误码表、幂等性、超时重试、版本兼容策略不在此重复**，
> 以 `../02-需求类/02-接口需求规格说明.md` 为准；本文档只做设计侧约束（适配层归属、缓存、熔断策略）。

### 3.4 接口设计通则

| 编号 | 通则 | 说明 |
|---|---|---|
| IF-D-01 | 所有外部依赖必须经**适配层模块**（`M60`–`M79`）封装 | 便于替换与 Mock，保证模块可独立测试（FC-5） |
| IF-D-02 | 模块间调用必须**无状态**或显式传递状态 | 避免隐式会话 |
| IF-D-03 | 跨模块异常必须映射为**统一错误码**（`<域前缀>-<4 位数字>`） | 禁止把下层异常原样抛出 |
| IF-D-04 | 任何契约变更 = 框架变更，**必须走 R5** | 见 `03-模块接口契约.md` |

---

## 4 运行设计

### 4.1 运行模块组合

| 场景编号 | 运行场景 | 参与模块 | 触发方式 | 说明 |
|---|---|---|---|---|
| RUN-01 | 用户登录 | `<M01>` → `<M20>` → `<M60>` → `<M40>` | 用户操作 | 主流程 |
| RUN-02 | 列表查询 | `<M01>` → `<M20>` → `<M40>` | 用户操作 | 性能敏感路径 |
| RUN-03 | 定时对账 | `<M20>` → `<M40>` → `<M70>` | 定时任务 | 无界面 |

### 4.2 运行控制

| 项 | 内容 |
|---|---|
| 启动方式 | 一条命令启动（`<命令>`）；配置来自 `<配置源>` |
| 模块装配 | 由模块注册表 + 依赖解析自动装配；顺序由拓扑排序决定 |
| 配置管理 | 环境变量 > 配置文件 > 默认值；敏感配置不落代码库 |
| 优雅停机 | 收到 SIGTERM 后停止接收新请求，等待在途请求（≤ 30s）后退出 |
| 失败启动 | 关键依赖（数据库）不可用时**拒绝启动**（fail-fast），而非带病运行 |

### 4.3 运行时间

| 场景编号 | 目标耗时 | 度量条件 | 关联需求 |
|---|---|---|---|
| RUN-02 | P95 ≤ 2s | 1000 万行 / 50 并发 | `<REQ-N-001>` |
| RUN-01 | P95 ≤ 500ms | 50 并发 | `<REQ-N-001>` |
| 启动 | ≤ 30s | 冷启动，单节点 | — |
| 定时任务窗口 | 每日 02:00–04:00 内完成 | 日均数据量 | — |

---

## 5 系统数据结构设计

### 5.1 逻辑结构设计要点

| 实体 | 主标识 | 关键属性 | 关系 | 关联需求 |
|---|---|---|---|---|
| User | `user_id` | `account`（唯一）、`password_hash`、`status`、`fail_count` | 1:N Role | `<REQ-F-001>` |
| Task | `task_id` | `tenant_id`、`status`、`owner`、`created_at` | N:1 User | `<REQ-N-001>` |

**状态流转（逻辑）**

```
Task: 待处理 ──→ 处理中 ──→ 已完成
         └──────────┴──────→ 已取消
```

### 5.2 物理结构设计要点

| 表/集合 | 关键字段 | 索引 | 分区/分片 | 预估容量 | 保留策略 |
|---|---|---|---|---|---|
| `t_user` | `user_id`(PK)、`account`(UK) | `uk_account` | — | 10 万行 | 永久 |
| `t_task` | `task_id`(PK)、`tenant_id`、`status`、`created_at` | `idx_tenant_status_created` | 按 `tenant_id` 哈希 | 1000 万行/年 | 5 年 |

> **性能关键路径**：`<REQ-N-001>` 的实现强依赖 `idx_tenant_status_created` 与游标分页；
> 若该索引在 1000 万行下无法满足 P95 ≤ 2s，将触发框架变更（`FC-1`）重新设计查询路径（见 §7 `A-03`）。

### 5.3 数据结构与程序的关系

| 数据结构 | 读模块 | 写模块 | 访问约束 | 关联接口 |
|---|---|---|---|---|
| `t_user` | `<M20>`、`<M40>` | `<M20>`（经 `<M40>`） | 仅经 `<M40>` 访问，禁止跨模块直连 | `<IF-001>` |
| `t_task` | `<M40>` | `<M40>` | 强制注入 `tenant_id` 隔离条件 | `<IF-003>` |

---

## 6 系统出错处理设计

### 6.1 出错信息

| 错误码 | 含义 | 触发场景 | 用户可见信息 | 日志级别 | 处置建议 |
|---|---|---|---|---|---|
| `AUTH-4011` | 凭证无效 | 账号/口令错误 | 账号或口令错误 | WARN | 提示重试 |
| `SYS-5001` | 依赖不可用 | 下游超时/熔断 | 服务暂时不可用，请稍后重试 | ERROR | 检查下游与熔断状态 |
| `SYS-5002` | 内部错误 | 未预期异常 | 系统繁忙，请提供 `trace_id` | ERROR | 依 `trace_id` 定位 |

**错误码编码规则**：`<域前缀 2–6 位大写字母>-<4 位数字>`，全项目唯一，**一经发布不得改语义、不得复用**。

### 6.2 补救措施

| 编号 | 故障场景 | 检测方式 | 补救措施 | 恢复目标 | 关联需求 |
|---|---|---|---|---|---|
| RM-01 | 应用节点宕机 | 健康检查 | 负载均衡摘除 + 自动拉起 | RTO ≤ 5min | `<REQ-N-012>` |
| RM-02 | 数据库主库故障 | 主备探测 | 切换备库（切换脚本 + 人工确认） | RTO ≤ 30min，RPO ≤ 5min | `<REQ-N-012>` |
| RM-03 | 消息积压 | 队列深度告警 | 扩容消费者 + 死信人工处置 | 积压清零 ≤ 2h | `<REQ-F-030>` |
| RM-04 | 数据误删 | 审计日志 + 备份 | 按备份点恢复指定表 | RTO ≤ 4h | `<REQ-N-012>` |

### 6.3 维护设计

| 项 | 内容 |
|---|---|
| 日志 | 结构化 JSON；统一 `trace_id`；敏感字段掩码；保留 ≥ 180 天 |
| 监控 | 接口成功率/延迟、资源使用率、队列深度、慢查询 |
| 配置 | 与代码分离，支持热加载项清单化 |
| 可测试性 | 每个模块可被独立测试（依赖可注入、外部依赖经适配层 Mock）；**这是 R4 的门禁项** |
| 向后兼容 | 破坏性变更必须提供并行期（≥ 90 天）与弃用通告 |
| 技术债 | 被 R5 驳回的架构改进记入技术债清单（如 `DEBT-<NNN>`），**不是丢弃** |

---

## 7 框架假设清单【本仓库定制章节 · S2 准出必填】

> 这是 S2 最容易被忽略、但对后续迭代**最关键**的一份**活文档**。
> 它是"框架—模块共演化"的输入：假设被推翻 → 触发框架变更（`FC-3`）→ 有据可依、范围可控。

| 假设编号 | 假设内容 | 置信度 | 若不成立的后果 | 验证时点 | 状态 |
|---|---|---|---|---|---|
| `A-01` | 上游数据源提供稳定的同步 HTTP 接口 | 中 | 需引入异步补偿机制，影响全部 IO 模块 | `<M02>` 实现时 | ⏳ 待验证 |
| `A-02` | 单机部署即可满足 200 并发，无需分布式锁 | 高 | 需引入协调服务，框架层大改 | S5 压测时 | ⏳ 待验证 |
| `A-03` | 单租户任务数据 < 100 万行，现有索引可满足 P95 ≤ 2s | 低 | 需改流式/分页处理，`<M40>` 与 `<IF-003>` 契约需扩展 | `<M04>` 实现时 | ❌ 已推翻 → 触发 `FC-1` |
| `A-04` | 全部业务事务为单库本地事务 | 高 | 需引入分布式事务，`<M20>` 事务边界重划 | S3 骨架时 | ✅ 已验证 |

**置信度**：高 = 有直接证据；中 = 有间接证据/经验；低 = 纯推测。
**状态**：✅ 已验证 / ⏳ 待验证 / ❌ 已推翻（推翻时必须提 `FC-3` 框架变更）。

### 7.1 硬规则

> **每个假设都必须有「验证时点」。没有验证时点的假设 = 定时炸弹。**
>
> 定时炸弹的典型爆炸方式：到 S5 集成测试时才发现假设不成立，此时已有 N 个模块建立在错误前提上，
> 改造成本 = 受影响模块数 × 单模块改造成本 + 回归成本，且往往已经逼近交付期。

| 检查项 | 判据 | 结论 |
|---|---|---|
| 每条假设是否有验证时点 | 100% 有（时点须具体到模块或阶段，禁止写"以后"） | ☐ |
| 每条假设是否有置信度 | 100% 有 | ☐ |
| 已推翻的假设是否已提 `FC-3` | 100% 已提 | ☐ |
| 假设是否有责任人 | 100% 有 | ☐ |

### 7.2 假设与框架版本的联动

| 假设状态变化 | 必须执行的动作 | 责任人 |
|---|---|---|
| 验证通过 | 更新状态为 ✅，在 §0.4 修订记录登记 | 实现者 |
| 验证失败 | 提 `FC-3` 框架变更（R5）→ 更新本表 + 更新 §2.4 结构 + 更新模块契约 → 框架版本 `vN+1` | 实现者 + 架构师 |
| 新增假设 | 立即登记（含验证时点），不积压 | 架构师 |

---

## 8 填写指引

1. **最常见错误：S2 想一次设计到位。** 大设计先行（反模式）会让 S2 停留数周，而抽象基于想象，实现一碰就碎。**S2 只定边界与契约，细节留到 S4 详细设计。**
2. **最常见错误：框架假设清单不写或写了没有验证时点。** 这是本仓库最看重的一节。没有假设清单，框架变更就变成"说不清为什么改"的返工；没有验证时点的假设就是定时炸弹。
3. **最常见错误：模块划分靠感觉，出现循环依赖或跨层直连。** 依赖图必须是有向无环图，表现层不得直连数据访问层。**R2 门禁对循环依赖一票否决**，且 CI 会自动检查。
4. **最常见错误：选了架构风格但不写理由，也不写被否决的方案。** "因为流行/因为大家都用"不是理由。必须写清选型理由与被否决方案，否则评审无法判定决策质量。
5. **最常见错误：接口定义只写成功路径。** 本文档只做接口设计侧约束，完整的错误码/幂等/超时/版本兼容在 `../02-需求类/02-接口需求规格说明.md` 与 `03-模块接口契约.md`；**三处的接口编号必须一一对应**。
6. **最常见错误：改了框架不改 HLD。** 每次 R5 批准的框架变更，必须在**同一次提交**里更新本文档、模块契约与 RTM。文档滞后提交数为 0 是健康度指标（见 `docs/01-流程与阶段/框架与模块共演化.md` §八）。

---

## 9 依据说明

- 本文档的**章节骨架**（引言 / 编写目的 / 背景 / 定义 / 参考资料 / 总体设计 / 需求规定 / 运行环境 / 基本设计概念和处理流程 / 结构 / 功能需求与程序的关系 / 人工处理过程 / 尚未解决的问题 / 接口设计 / 用户接口 / 内部接口 / 外部接口 / 运行设计 / 运行模块组合 / 运行控制 / 运行时间 / 系统数据结构设计 / 逻辑结构设计要点 / 物理结构设计要点 / 数据结构与程序的关系 / 系统出错处理设计 / 出错信息 / 补救措施 / 维护设计）按 **GB/T 8567-2006《计算机软件文档编制规范》** 对**概要设计说明书**这一文档种类的要求组织；需求映射与可追溯性参照 GB/T 9385-2008。
- **保守处理声明**：GB/T 8567-2006 中概要设计说明书的精确分节标题、层级与条款编号，本模板未逐字复制（国标正文有版权，且本仓库不复制标准正文）。§2.4.1 的模块号分层分带、§2.4.4 框架—模块共演化接口、**§7 框架假设清单**、§3.4 接口设计通则，均为**本仓库的工程强化项**，不是标准原文要求。**章节骨架按 GB/T 8567-2006 对概要设计说明书文档种类的要求组织，实施时请以标准正文为准。**
- 标准状态核验日期：2026-02；使用前请到[国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/gb/)复核现行性。
