# 版本说明

> **文档编号**：`<项目代号>-RN-<版本号>-V<主版本>.<次版本>`
> **编号规则**：`<项目代号>-<类别码>-<三位序号>-V<主版本>.<次版本>`；本文件类别码 **RN**（Release Note）。
> 因版本说明与发布版本一一对应，编号中直接嵌入**被说明的版本号**，如 `<项目代号>-RN-2.1.0-V1.0`。
> **适用阶段**：**S7 交付与维护**（每次版本发布前完稿，作为 **R8 发布评审**的必审材料）。
> **依据标准 / 依据文件**：
> - **GB/T 8566-2022**《系统与软件工程 软件生存周期过程》——配置管理过程、发布与交付过程；
> - **GB/T 8567-2006**《计算机软件文档编制规范》——版本说明类文档的编写要求；
> - **GB/T 38634.2-2020**《系统与软件工程 软件测试 第2部分：测试过程》——回归测试结论引用（见 `docs/03-测试与回归/缺陷管理与回归测试.md`）；
> - **`templates/07-配置与发布类/01-软件配置管理计划.md`**——配置项标识、基线、版本号规范、制品记录要求的来源；
> - `docs/04-配置与版本/配置管理与版本化.md`——本仓库版本号与基线约定的落地细则；
> - GB/T 12505-90《计算机软件配置管理计划规范》——**1990 年老标准**，仅采其五要素框架（配置项标识、基线管理、变更控制、状态记录、配置审计），实现方式按现代工程实践。
>
> **状态**：**模板**（未填写）。发布前经 R8 发布评审批准，随产品基线 `product/vX.Y.Z` 归档。
>
> **说明**：本模板不复制国标正文。**GB/T 15532-2008 是 2008 年老标准，新项目应以 GB/T 38634 系列为准**。

---

## 1 版本标识

| 项 | 内容 |
|---|---|
| 产品名称 | `<项目名称>` |
| **版本号** | **`<MAJOR.MINOR.PATCH>`**（如 `2.1.0`；预发布后缀 `-alpha.N` / `-beta.N` / `-rc.N`） |
| **发布日期** | `<YYYY-MM-DD>` |
| **版本类型** | ☐ **MAJOR**（主版本）　☐ **MINOR**（次版本）　☐ **PATCH**（修订号） |
| **递增理由** | `<一句话说明为何是该类型，见第 2 章>` |
| 上一版本号 | `<X.Y.Z>`（首个版本填"无"） |
| 框架版本 | `<framework/v0.x>` |
| **版本标签（Git Tag）** | `product/v<X.Y.Z>` |
| **构建号** | `<build-NNN>` / CI 运行编号 `<run-id>` |
| 发布评审编号 | `<项目代号>-RV-R8-<序号>-V<主>.<次>` |
| 变更申请编号 | `<FC-<年>-<序号> / <变更单编号>` |
| 编制人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 批准人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |

---

## 2 版本类型与递增理由

### 2.1 语义化版本递增规则

| 位 | 名称 | 递增条件 | 示例 | 兼容性含义 |
|---|---|---|---|---|
| **MAJOR** | 主版本 | **不兼容**的接口 / 数据 / 行为变更 | `1.4.2` → `2.0.0` | ⚠️ 使用方**必须**适配 |
| **MINOR** | 次版本 | **向后兼容**的功能新增 | `1.4.2` → `1.5.0` | ✅ 可直接升级 |
| **PATCH** | 修订号 | **向后兼容**的缺陷修复 | `1.4.2` → `1.4.3` | ✅ 可直接升级 |

预发布后缀：`-alpha.N`（内测）→ `-beta.N`（公测）→ `-rc.N`（候选发布）。
构建元数据：`+build.NNN`（不参与版本比较）。

### 2.2 版本递增决策核对

| 变更性质 | 产品版本 | 框架版本 | 本次是否命中 | 说明 |
|---|---|---|---|---|
| 修 bug，契约不变 | PATCH | 不变 | ☐ | |
| 新增模块（不改既有契约） | MINOR | 可能 MINOR | ☐ | |
| 契约扩展字段（兼容） | MINOR | MINOR | ☐ | |
| 契约语义变更（不兼容） | **MAJOR** | **MAJOR** | ☐ | |
| 数据模型不兼容变更 | **MAJOR** | 可能 MAJOR | ☐ | |
| 仅文档 / 日志 / 配置默认值 | PATCH | 不变 | ☐ | |

> **框架版本与产品版本分离**：框架版本从 `v0` 起，用 `vMAJOR.MINOR` 两位。
> **不要过早宣布框架 v1.0**——一旦 v1.0 即意味着契约冻结，此后任何破坏性变更
> 都必须走完整变更流程并通知全部使用方。

### 2.3 递增理由说明

```
<用 2–4 句话说明：本次为何判定为该版本类型。
 若为 MAJOR，必须逐条列出不兼容点及其对使用方的影响。
 若为 MINOR/PATCH，必须确认不存在破坏性变更，并说明核对方式。>
```

### 2.4 不兼容变更清单（**仅 MAJOR 版本填写**）

| 序号 | 不兼容点 | 类型 | 影响范围 | 使用方需做的适配 | 是否已通知全部使用方 |
|---|---|---|---|---|---|
| 1 | `<如：接口 IF-007 的响应结构由数组改为对象>` | 接口 / 数据 / 行为 | `<受影响模块/外部系统>` | `<适配动作>` | ☐ 是 ☐ 否 |
| 2 | `<不兼容点>` | `<类型>` | `<范围>` | `<适配动作>` | ☐ |

> **破坏性变更必须通知全部使用方。** 未完成通知的 MAJOR 版本不得发布。

---

## 3 变更内容

### 3.1 新增功能

| 序号 | 变更编号 | 关联需求编号 | 关联模块 | 功能描述 | 提交/PR | 验收用例 |
|---|---|---|---|---|---|---|
| 1 | `<FC-<年>-<序号> / PR-<号>>` | `<REQ-F-001>` | `<M01>` | `<一句话描述新增能力>` | `<commit-hash>` | `<TC-001>` |
| 2 | `<变更编号>` | `<REQ-F-012>` | `<M03>` | `<描述>` | `<提交>` | `<TC-003>` |

### 3.2 缺陷修复

| 序号 | 缺陷编号 | 关联需求 | 关联模块 | 严重级别 | 问题现象 | 修复说明 | 回归策略 | 回归结果 |
|---|---|---|---|---|---|---|---|---|
| 1 | `<BUG-001>` | `<REQ-F-012>` | `<M01>` | `P1` | `<现象>` | `<根因与修复方式>` | `R-B 模块回归` | ☐ 通过 |
| 2 | `<BUG-002>` | `<REQ-F-005>` | `<M02>` | `P2` | `<现象>` | `<修复说明>` | `R-A 冒烟回归` | ☐ 通过 |

| 回归策略说明 | 适用场景 |
|---|---|
| `R-A 冒烟回归` | 仅文案、日志、配置默认值等无逻辑变更 |
| `R-B 模块回归` | 单模块内部修改，接口契约未变 |
| `R-C 契约回归` | 接口契约变更（= 框架变更） |
| `R-D 全量回归` | 框架结构性变更、数据模型变更、发布前 |

### 3.3 框架变更

| 序号 | 变更编号 | 触发条件 | 受影响模块 | 受影响需求 | 框架版本变化 | 是否破坏性 | 关联评审 |
|---|---|---|---|---|---|---|---|
| 1 | `<FC-<年>-<序号>>` | `FC-1 契约不足 / FC-2 职责错位 / FC-3 假设被推翻 / FC-4 重复实现 / FC-5 测试不可达` | `<M01>, <M05>` | `<REQ-F-012>` | `v0.3` → `v0.4` | ☐ 是 ☐ 否 | `<RV-R5-...>` |
| 2 | `<变更编号>` | `<触发条件>` | `<模块>` | `<需求>` | `<版本变化>` | ☐ | `<评审编号>` |

> **FC-6 个人偏好类变更不进入发版内容**，应记入技术债清单（`DEBT-xxx`）。
> 若本次版本包含框架变更，**必须**同步确认 HLD 与模块接口契约已更新（无文档漂移）。

### 3.4 文档变更

| 序号 | 文档编号 | 文档名称 | 版本变化 | 变更内容 | 关联变更编号 |
|---|---|---|---|---|---|
| 1 | `<编号>` | `<软件需求规格说明>` | `v1.0` → `v1.1` | `<变更摘要>` | `<FC-<年>-<序号>>` |
| 2 | `<编号>` | `<软件概要设计说明>` | `v0.3` → `v0.4` | `<变更摘要>` | `<变更编号>` |
| 3 | `<编号>` | `<用户手册>` | `<版本变化>` | `<变更摘要>` | `<变更编号>` |

### 3.5 变更统计汇总

| 类别 | 数量 | 关联需求数 | 关联缺陷数 | 备注 |
|---|---|---|---|---|
| 新增功能 | `<n>` | `<n>` | — | |
| 缺陷修复 | `<n>` | — | `<n>` | |
| 框架变更 | `<n>` | `<n>` | — | 破坏性：`<n>` |
| 文档变更 | `<n>` | — | — | |
| **合计** | `<n>` | `<n>` | `<n>` | |

### 3.6 变更追溯完整性核对

| 核对项 | 要求 | 实际 | 判定 |
|---|---|---|---|
| 每项变更关联到需求编号 | 是 | ☐ | ☐ |
| 每项修复关联到缺陷编号 | 是 | ☐ | ☐ |
| 每项框架变更关联到变更编号与评审 | 是 | ☐ | ☐ |
| 每项变更已追溯至设计/代码/测试 | 是 | ☐ | ☐ |
| 追溯矩阵已更新 | 是（`tools/trace_matrix.py` 通过） | ☐ | ☐ |

---

## 4 已知问题

> **已知问题是交付方的书面告知义务。** 未列入本节的遗留缺陷，在交付后被使用时
> 将被视为"未声明的质量缺陷"，责任归属将产生争议。

### 4.1 遗留缺陷

| 序号 | 缺陷编号 | 模块 | 严重级别 | 问题描述 | 影响范围 | 规避方式 | 计划修复版本 | 是否经批准 |
|---|---|---|---|---|---|---|---|---|
| 1 | `<BUG-003>` | `<M02>` | `P2` | `<描述>` | `<影响>` | `<规避方式>` | `<X.Y.Z>` | ☐ |
| 2 | `<BUG-004>` | `<M05>` | `P3` | `<描述>` | `<影响>` | `<规避方式>` | `<版本>` | ☐ |

### 4.2 已知限制（非缺陷）

| 序号 | 限制项 | 说明 | 触发条件 | 规避方式 |
|---|---|---|---|---|
| 1 | `<如：单笔上限 100000.00>` | `<业务约定限制>` | `<条件>` | `<方式>` |
| 2 | `<如：不支持 IE 浏览器>` | `<兼容性限制>` | `<条件>` | `<方式>` |
| 3 | `<如：单机部署，不支持水平扩展>` | `<架构限制>` | `<条件>` | `<方式>` |

### 4.3 技术债

| 编号 | 内容 | 来源 | 影响 | 计划处理版本 |
|---|---|---|---|---|
| `DEBT-011` | `<重构/优化项>` | `<R5 驳回的变更 / 缺陷规避>` | `<影响>` | `<版本号>` |

### 4.4 P0/P1 缺陷核查（**发布硬闸**）

| 级别 | 遗留数量 | 发布要求 | 判定 |
|---|---|---|---|
| **P0 致命** | `<n>` | **必须为 0** | ☐ |
| **P1 严重** | `<n>` | **必须为 0** | ☐ |

> **P0/P1 不许带病出厂。** 若确需带病发布，必须由甲方/产品**书面确认风险**，
> 转为"已知问题"并列入 4.1 节，同时说明规避方式与修复计划。
> 口头同意不构成批准。

---

## 5 升级与回滚注意事项

### 5.1 升级前检查

| 序号 | 检查项 | 要求 | 说明 |
|---|---|---|---|
| 1 | 当前版本 | 从 `<X.Y.Z>` 升级 | 跨 MAJOR 升级需评估不兼容点 |
| 2 | 环境依赖 | `<运行时/中间件/数据库版本要求>` | 不满足需先升级依赖 |
| 3 | 配置变更 | ☐ 无 ☐ 有：`<列出新增/修改/废弃的配置项>` | 必须逐项说明 |
| 4 | 数据迁移 | ☐ 不需要 ☐ 需要：`<迁移脚本路径>` | **迁移前必须备份** |
| 5 | 依赖清单 | 锁定文件已更新 | 保证可复现构建 |
| 6 | 密钥/凭据 | ☐ 无变化 ☐ 有变化：`<说明>` | 不得在文档中记录真实密钥 |
| 7 | 停机窗口 | `<是否需要停机；预计时长>` | |
| 8 | 备份 | ☐ 已完成数据与配置备份 | 回滚的前提 |

### 5.2 升级步骤

| 步骤 | 操作 | 预期结果 | 验证方式 | 责任人 |
|---|---|---|---|---|
| 1 | 备份数据与配置 | 备份文件生成 | 校验备份可恢复 | `<姓名>` |
| 2 | 停止服务 | 服务停止，无残留进程 | 进程检查 | `<姓名>` |
| 3 | 部署新版本制品 | 制品哈希校验一致 | 哈希比对 | `<姓名>` |
| 4 | 执行数据迁移（如需） | 迁移脚本成功退出 | 迁移日志 + 数据抽样核对 | `<姓名>` |
| 5 | 更新配置 | 配置项生效 | 配置加载日志 | `<姓名>` |
| 6 | 启动服务 | 服务正常启动 | 健康检查通过 | `<姓名>` |
| 7 | **执行冒烟测试** | 冒烟用例全部通过 | L0 冒烟记录 | `<姓名>` |
| 8 | 观察监控 | `<n>` 分钟内无异常告警 | 监控面板 | `<姓名>` |

### 5.3 回滚方案

| 项 | 内容 |
|---|---|
| **回滚触发条件** | `<明确条件，如：冒烟失败 / 关键功能不可用 / P0 缺陷出现 / 监控告警持续 N 分钟>` |
| **回滚目标版本** | `<X.Y.Z>`（制品必须仍在制品库中可获取） |
| **回滚步骤** | `1. <停止服务> 2. <还原上一版本制品> 3. <回滚数据迁移（脚本路径）> 4. <还原配置> 5. <启动并冒烟>` |
| **数据回滚方案** | `<如：使用迁移前备份还原；是否可逆；不可逆变更的处置>` |
| **回滚预计耗时** | `<n>` 分钟 |
| **回滚验证方式** | `<冒烟测试 + 关键功能验证>` |
| **回滚演练记录** | ☐ 已在测试环境演练（日期 `<YYYY-MM-DD>`） ☐ 未演练（说明：`<原因>`） |
| **回滚决策人** | `<姓名>` |

> **回滚方案必须确认可行，否则不得发布（R8 准出项）。**
> 只写"如有问题则回滚"不算方案——必须写明触发条件、步骤、数据处置与验证方式。
> **涉及不可逆数据迁移的变更，回滚可行性必须在发布前明确结论。**

### 5.4 升级风险提示

| 风险 | 可能性 | 影响 | 应对 |
|---|---|---|---|
| `<如：数据迁移耗时超预期>` | `<高/中/低>` | `<高/中/低>` | `<应对措施>` |
| `<如：新旧版本并行期间数据不一致>` | `<可能性>` | `<影响>` | `<措施>` |

---

## 6 兼容性说明

| 兼容性维度 | 本版本情况 | 是否兼容上一版本 | 说明 |
|---|---|---|---|
| **接口兼容性** | `<接口清单与版本>` | ☐ 完全兼容 ☐ 兼容（新增字段） ☐ **不兼容** | `<不兼容点>` |
| **数据格式兼容性** | `<数据模型/文件格式变化>` | ☐ 兼容 ☐ **不兼容** | `<说明>` |
| **配置兼容性** | `<新增/废弃配置项>` | ☐ 兼容（旧配置仍有效） ☐ **不兼容** | `<废弃项需迁移>` |
| **依赖兼容性** | `<运行时/中间件/数据库版本要求>` | ☐ 兼容 ☐ 要求升级 | `<最低版本要求>` |
| **操作系统兼容性** | `<支持的 OS 与版本>` | ☐ 兼容 | |
| **浏览器/终端兼容性** | `<支持范围>` | ☐ 兼容 | |
| **向后兼容** | 新版本读取旧数据 | ☐ 支持 ☐ 不支持 | `<说明>` |
| **向前兼容** | 旧版本读取新数据 | ☐ 支持 ☐ 不支持 | `<说明>` |

### 6.1 兼容性矩阵

| 组件 | 支持版本范围 | 本次变化 | 备注 |
|---|---|---|---|
| 运行时 | `<最低版本> – <最高验证版本>` | ☐ 无变化 ☐ 提高下限 | |
| 数据库 | `<类型 + 版本范围>` | ☐ 无变化 ☐ `<变化>` | |
| 中间件 | `<类型 + 版本范围>` | ☐ 无变化 ☐ `<变化>` | |
| 客户端/浏览器 | `<范围>` | ☐ 无变化 ☐ `<变化>` | |

---

## 7 制品与校验信息

> **制品（构建产物）不入代码库**，须存放于制品库或 Release 附件，
> 并**记录版本标签与哈希**以便校验。无哈希的制品无法证明与其源代码的对应关系。

### 7.1 制品清单

| 序号 | 制品名称 | 文件名 | 格式 | 大小 | 存放位置 | SHA-256 哈希 |
|---|---|---|---|---|---|---|
| 1 | `<产品主程序>` | `<name-X.Y.Z.zip>` | `<zip/jar/wheel/镜像>` | `<n>` MB | `<制品库/Release 链接>` | `<sha256>` |
| 2 | `<数据库迁移脚本>` | `<migrate-X.Y.Z.sql>` | `sql` | `<n>` KB | `<位置>` | `<sha256>` |
| 3 | `<部署脚本/配置模板>` | `<deploy-X.Y.Z.tar.gz>` | `<格式>` | `<n>` KB | `<位置>` | `<sha256>` |
| 4 | `<容器镜像>` | `<image:X.Y.Z>` | `OCI` | `<n>` MB | `<镜像仓库>` | `<digest>` |

### 7.2 版本控制与构建信息

| 项 | 内容 |
|---|---|
| **版本标签** | `product/v<X.Y.Z>`（☐ 已打标签 ☐ 已推送） |
| **框架基线标签** | `framework/v0.x` |
| **构建号** | `<build-NNN>` |
| 构建时间 | `<YYYY-MM-DD HH:MM>` |
| 构建流水线 | `<CI 流水线名称>` |
| 构建运行编号 | `<run-id>` |
| 构建日志路径 | `<链接或路径>` |
| 源代码提交号 | `<commit-hash>`（标签指向的提交） |
| 构建可复现性 | ☐ 由干净环境构建 ☐ 依赖锁定文件已入库 |

### 7.3 交付物清单核对

| 序号 | 交付物 | 文档/制品编号 | 版本 | 是否齐备 | 备注 |
|---|---|---|---|---|---|
| 1 | 软件需求规格说明 | `<编号>` | `<版本>` | ☐ | |
| 2 | 软件概要设计说明 | `<编号>` | `<版本>` | ☐ | |
| 3 | 软件详细设计说明 | `<编号>` | `<版本>` | ☐ | |
| 4 | 测试计划 / 说明 / 报告 | `<编号>` | `<版本>` | ☐ | |
| 5 | 用户手册 / 操作手册 | `<编号>` | `<版本>` | ☐ | |
| 6 | 部署与安装说明 | `<编号>` | `<版本>` | ☐ | |
| 7 | 版本说明（本文件） | `<编号>` | `<版本>` | ☐ | |
| 8 | 制品包 | `<制品名>` | `<X.Y.Z>` | ☐ | 含哈希 |
| 9 | 源码 | `<commit-hash>` | — | ☐ | 标签已打 |

### 7.4 配置审计核对（R8 前执行）

| 序号 | 审计项 | 判定 | 证据 |
|---|---|---|---|
| 1 | 所有配置项均已纳入版本控制（无游离文件） | ☐ | |
| 2 | 无密钥/凭据入库（含历史提交扫描） | ☐ | |
| 3 | 依赖锁定文件已更新且可复现构建 | ☐ | |
| 4 | 所有基线标签符合命名规范且指向正确提交 | ☐ | |
| 5 | 变更申请与影响分析齐全，均已批准 | ☐ | |
| 6 | 每次变更都有对应的回归测试记录 | ☐ | |
| 7 | 文档与代码版本一致（无漂移） | ☐ | |
| 8 | 追溯矩阵完整（需求→设计→代码→测试 双向可查） | ☐ | |
| 9 | 版本说明含变更内容、已知问题、升级与回滚注意 | ☐ | |
| 10 | 制品哈希已记录，可校验 | ☐ | |

---

## 8 依据的配置管理计划条目

> 本版本说明的各项内容，其**要求的来源**是配置管理计划。逐条对应，便于审计追溯。

| 序号 | 本文件章节 | 配置管理计划条目 | 条目要求摘要 | 符合性 |
|---|---|---|---|---|
| 1 | 1 版本标识 | `<CM-PLAN> §<x> 配置项标识` | 配置项须有唯一标识，版本号须符合规范 | ☐ |
| 2 | 2 版本类型与递增理由 | `<CM-PLAN> §<x> 版本号规范` | 语义化版本；框架版本与产品版本分离 | ☐ |
| 3 | 3 变更内容 | `<CM-PLAN> §<x> 变更控制` | 变更须经申请、评审、批准并留痕 | ☐ |
| 4 | 4 已知问题 | `<CM-PLAN> §<x> 状态记录` | 变更历史与遗留问题须记录 | ☐ |
| 5 | 5 升级与回滚 | `<CM-PLAN> §<x> 发布与交付 / 基线管理` | 发布须有回滚方案；基线须打标签冻结 | ☐ |
| 6 | 6 兼容性说明 | `<CM-PLAN> §<x> 版本号规范` | 版本类型须反映兼容性 | ☐ |
| 7 | 7 制品与校验信息 | `<CM-PLAN> §<x> 制品与交付物管理` | 构建产物须记录版本标签与哈希 | ☐ |
| 8 | 7.4 配置审计 | `<CM-PLAN> §<x> 配置审计` | 发布前须执行配置审计 | ☐ |
| 9 | 全文 | `<CM-PLAN> §<x> 基线管理` | 产品基线 `product/vX.Y.Z` 建立与冻结 | ☐ |

| 基线信息 | 内容 |
|---|---|
| 产品基线标签 | `product/v<X.Y.Z>` |
| 基线冻结日期 | `<YYYY-MM-DD>` |
| 冻结内容 | 上表 7.3 全部交付物 + 制品哈希 |
| 基线变更要求 | 产品基线**只能通过新版本发布演进**，不得静默修改 |

---

## 9 发布结论（R8 发布评审）

| 序号 | R8 准出项 | 判据 | 实际 | 判定 |
|---|---|---|---|---|
| 1 | 变更申请与影响分析已批准 | 批准人签字 | `<编号/日期>` | ☐ |
| 2 | 回归测试按影响范围执行完毕且通过 | 回归记录 | `<策略与结果>` | ☐ |
| 3 | 版本号符合语义化规范 | 递增类型与变更性质一致 | `<X.Y.Z / 类型>` | ☐ |
| 4 | 版本说明已完稿 | 含变更内容、已知问题、升级注意 | 本文件 | ☐ |
| 5 | 回滚方案已确认可行 | 触发条件/步骤/数据处置明确 | 见 5.3 | ☐ |
| 6 | P0/P1 遗留缺陷为 0 | 遗留数 = 0 | `<n>/<n>` | ☐ |
| 7 | 配置审计通过 | 7.4 全部项通过 | `<n>/10` | ☐ |

| 项 | 内容 |
|---|---|
| **发布结论** | ☐ **发布**　☐ **不发布**（退回修改后重新评审） |
| 若不发布，原因 | `<明确写清哪项未满足>` |
| 附加条件 | `<如有条件，逐条列出条件、责任人与闭环期限>` |

### 签字栏

| 角色 | 姓名 | 结论（发布/不发布） | 签字 | 日期 |
|---|---|---|---|---|
| 编制人 | `<姓名>` | — | | `<YYYY-MM-DD>` |
| 开发负责人 | `<姓名>` | ☐ 发布 ☐ 不发布 | | `<YYYY-MM-DD>` |
| 测试负责人 | `<姓名>` | ☐ 发布 ☐ 不发布 | | `<YYYY-MM-DD>` |
| 运维负责人 | `<姓名>` | ☐ 发布 ☐ 不发布 | | `<YYYY-MM-DD>` |
| **技术负责人（主持）** | `<姓名>` | ☐ 发布 ☐ 不发布 | | `<YYYY-MM-DD>` |
| 配置管理员（存档） | `<姓名>` | — | | `<YYYY-MM-DD>` |

---

## 10 填写指引（常见错误）

1. **版本类型与实际变更性质不符**——修了个 bug 却发 MINOR、加了新功能却发 PATCH，会让使用方无法判断升级风险。**每一次"契约语义变更"都必须是 MAJOR**，并逐条列出不兼容点。判定依据填在第 2.2 节的决策核对表。
2. **已知问题章节留空或只写"无"**——遗留缺陷与已知限制**必须如实列出**。交付后暴露的未声明问题会被视为质量缺陷，责任归属产生争议。P0/P1 若需带病发布，必须有甲方/产品**书面确认**，口头同意不构成批准。
3. **回滚方案只写"如有问题则回滚"**——这不是方案。必须写明**触发条件、回滚步骤、数据回滚处置、预计耗时、验证方式与决策人**。涉及**不可逆数据迁移**的变更，回滚可行性必须在发布前给出明确结论。
4. **制品记录缺哈希**——只写文件名与下载链接无法证明制品与其源代码的对应关系，也无法在部署后校验一致性。**每个制品必须记录 SHA-256 哈希**（容器镜像记录 digest）。
5. **变更列表缺少关联编号**——每项变更必须关联**变更编号 + 需求编号（或缺陷编号）**。缺少关联会导致追溯矩阵断裂，"这个版本到底改了什么"将无法回答。填完请跑 `python tools/trace_matrix.py` 验证。
6. **升级注意事项漏写配置与数据迁移**——新增/修改/废弃的配置项、是否需要数据迁移、是否可逆，这三项是最常被漏掉且最容易导致升级失败的内容。**迁移前必须先备份**，并写入升级步骤。
7. **文档漂移未核对**——若本版本包含框架变更，必须确认 HLD 与模块接口契约已同步更新。**代码改了文档没改**是配置管理失效的头号征兆，一旦文档不可信，整套追溯体系归零。

---

## 附：本模板的保守处理声明

| 项 | 保守处理方式 | 原因 |
|---|---|---|
| 国标条款编号 | 不引用具体条款号，只声明依据 GB/T 8566-2022 配置管理过程与 GB/T 8567-2006 编写要求 | 条款编号须以标准正文为准 |
| **配置管理计划条目引用** | 第 8 章以**条目名称 + 占位章节号 `<§<x>>`** 形式引用，**不预设具体章节编号** | 配置管理计划的章节结构由项目自行定义；预填编号会与项目实际文档不符 |
| GB/T 12505-90 | 明确标注为 **1990 年老标准**，仅采其五要素框架（配置项标识、基线管理、变更控制、状态记录、配置审计） | 该标准实质内容已过时（无持续集成、无制品仓库概念），实现方式按现代工程实践 |
| GB/T 15532-2008 | 明确标注为老标准，不作主依据 | 防止新项目引用过时标准 |
| 语义化版本规则 | 采用业界通行的语义化版本约定（本仓库既定约定），**不声称来自国标** | 版本号格式属工程约定，GB/T 8566-2022 规定配置管理过程但**不规定版本号格式** |
| 制品哈希算法 | 统一采用 SHA-256（容器镜像用 digest） | 作为最低要求；若项目制品库另有约定可替换，但**不得省略哈希** |
