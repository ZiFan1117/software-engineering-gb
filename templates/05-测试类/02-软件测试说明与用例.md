# 软件测试说明与用例

> **文档编号**：`<项目代号>-TS-<序号>-V<主版本>.<次版本>`
> **编号规则**：`<项目代号>-<类别码>-<三位序号>-V<主版本>.<次版本>`；本文件类别码 **TS**（Test Specification），
> 内含**测试设计说明 / 测试用例说明 / 测试规程说明**三类内容。
> **适用阶段**：**S5 集成与系统测试**（用例设计在 S4 后期启动，S6 验收用例在此扩展）。
> **依据标准**：
> - **GB/T 38634.3-2020**《系统与软件工程 软件测试 第3部分：测试文档》——测试设计说明、测试用例说明、测试规程说明的内容要求（**主依据**）；
> - **GB/T 38634.4-2020**《第4部分：测试技术》——等价类划分、边界值、判定表、状态迁移、场景法、错误推测等技术的选择依据；
> - **GB/T 38634.2-2020**《第2部分：测试过程》——测试设计与实现活动的过程要求；
> - **GB/T 38634.1-2020**《第1部分：概念和定义》——测试项、测试用例、测试规程等术语统一；
> - **GB/T 25000.51-2016**——验收用例的质量特性归属；
> - GB/T 9385-2008《计算机软件需求规格说明规范》——用例必须回溯到可验证需求。
>
> **状态**：**模板**（未填写）。填写完成后随 `test-baseline/v1.0` 冻结；用例变更须说明理由并留痕。
>
> **说明**：本模板不复制国标正文。章节为 GB/T 38634.3-2020 文档内容的**落地映射**，条款编号请以标准正文为准。
> **GB/T 15532-2008 是 2008 年老标准，新项目应以 GB/T 38634 系列为准**。

---

## 1 引言

### 1.1 标识

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-TS-<序号>-V<主>.<次>` |
| 被测系统版本 | `<产品版本号>` / 框架版本 `<framework/v0.x>` |
| 对应测试计划 | `<项目代号>-TP-<序号>-V<主>.<次>` |
| 编制人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审状态 | 草稿 · 评审中 · 已批准 |

### 1.2 目的与范围

| 项 | 内容 |
|---|---|
| 本文档目的 | 把已基线需求转化为**可独立复现**的测试用例，使执行者不看代码即可执行并判定 |
| 覆盖范围 | L0–L4 各级别的用例设计；验收用例按 GB/T 25000.51-2016 的质量特性归类 |
| 不覆盖范围 | `<如：性能脚本实现细节、缺陷管理流程，见对应文档>` |

### 1.3 用例编号规则

| 前缀 | 含义 | 示例 |
|---|---|---|
| `<TC-F-xxx>` | 功能测试用例 | `<TC-F-001>` |
| `<TC-B-xxx>` | 边界测试用例 | `<TC-B-001>` |
| `<TC-E-xxx>` | 异常/容错测试用例 | `<TC-E-001>` |
| `<TC-P-xxx>` | 性能测试用例 | `<TC-P-001>` |
| `<TC-C-xxx>` | 并发测试用例 | `<TC-C-001>` |
| `<TC-R-xxx>` | 恢复测试用例 | `<TC-R-001>` |
| `<TC-S-xxx>` | 安全性测试用例 | `<TC-S-001>` |
| `<TC-M-xxx>` | 兼容性测试用例 | `<TC-M-001>` |

> **规则**：用例编号**全域唯一、一经分配永不复用**。用例作废时保留编号并标记"已作废 + 理由"，
> 禁止把编号回收给新用例——否则历史测试记录将无法解释。

---

## 2 引用文件

| 编号 | 名称 | 版本 | 用途 |
|---|---|---|---|
| `<REQ-SPEC>` | 软件需求规格说明 | `req-baseline/v1.0` | 用例的需求来源与验收准则 |
| `<RTM>` | 需求追溯矩阵 | `req-baseline/v1.0` | 需求覆盖率核算 |
| `<IF-SPEC>` | 接口需求规格说明 | `req-baseline/v1.0` | 接口用例与异常传递约定 |
| `<LLD>` | 软件详细设计说明 | `<版本>` | 测试项划分与白盒用例设计输入 |
| `<TEST-PLAN>` | 软件测试计划 | `<编号>` | 级别、类型、准出准则 |
| `<DATA-SPEC>` | 测试数据说明/生成脚本 | `<版本>` | 测试数据的可复现来源 |

---

## 3 测试项与测试条件

### 3.1 测试项清单

**测试项**（test item）是被测对象的可标识部分。测试项划分过粗会导致覆盖无法核算。

| 测试项编号 | 测试项名称 | 关联模块 | 关联需求 | 覆盖级别 | 用例数 |
|---|---|---|---|---|---|
| `TI-01` | `<如：订单提交>` | `M01` | `REQ-F-001`, `REQ-F-002` | L1–L4 | `<n>` |
| `TI-02` | `<如：金额校验>` | `M01`, `M03` | `REQ-F-012` | L1–L3 | `<n>` |
| `TI-03` | `<如：支付回调处理>` | `M05` | `REQ-F-019`, `IF-007` | L2–L3 | `<n>` |
| `<TBD-01>` | `<待定测试项>` | `<模块>` | `<需求>` | `<级别>` | `<n>` |

### 3.2 测试条件

| 测试条件编号 | 测试项 | 条件描述 | 类型（正常/边界/异常/空值） | 对应用例 |
|---|---|---|---|---|
| `COND-01` | `TI-02` | 金额在合法区间内 | 正常 | `<TC-F-001>` |
| `COND-02` | `TI-02` | 金额恰等于单笔上限 | 边界 | `<TC-B-001>` |
| `COND-03` | `TI-02` | 金额超出单笔上限 | 边界（拒绝侧） | `<TC-E-001>` |
| `COND-04` | `TI-02` | 金额为 0 / null / 空字符串 | **空值** | `<TC-B-002>` |

> **强制要求**：每个测试项**至少覆盖四类路径**：正常、边界、异常、**空值**。
> 只测正常路径的测试项视为**未完成设计**，不得进入执行阶段。

---

## 4 测试设计技术选择

依据 **GB/T 38634.4-2020** 选择技术。**技术选择必须有理由**，写在表中"选择理由"列。

| 设计技术 | 适用场景 | 不适用场景 | 本项目选择 | 选择理由 |
|---|---|---|---|---|
| **等价类划分** | 输入域可划分为若干"处理逻辑相同"的区间；参数多、组合爆炸需先收敛 | 各输入值处理逻辑互不相同、无法归类的场景 | ☐ 采用 | `<如：金额、数量等连续输入的初步收敛>` |
| **边界值分析** | 存在明确上下限、长度限制、精度要求的输入；**缺陷高发区** | 无明确边界或边界不可知的输入 | ☐ 采用 | `<如：单笔上限 100000.00、字符串长度上限>` |
| **判定表** | 多个条件组合决定不同动作（业务规则矩阵、权限矩阵） | 条件之间无组合关系、纯顺序流程 | ☐ 采用 | `<如：折扣规则由会员等级×金额区间共同决定>` |
| **状态迁移** | 对象有明确状态机（订单、工单、审批流、连接状态） | 无状态或状态不可枚举的处理 | ☐ 采用 | `<如：订单状态 待支付→已支付→已发货→已完成>` |
| **场景法** | 端到端业务流程、跨模块串联、用户真实使用路径（L3/L4 主力技术） | 单函数级白盒验证 | ☐ 采用 | `<如：主流程下单到支付完成全链路>` |
| **错误推测** | 基于经验预判易错点；探索性测试；故障注入的选点 | **不可作为唯一技术**——覆盖率无法度量 | ☐ 采用 | `<如：并发下重复提交、时钟回拨、依赖超时>` |

### 4.1 技术覆盖度检查（GB/T 38634.4-2020 要求基于风险选择）

| 检查项 | 要求 | 本项目情况 |
|---|---|---|
| 黑盒技术覆盖 | 至少覆盖等价类划分 + 边界值分析 + 场景法 | ☐ |
| 白盒技术覆盖 | L1 单元测试须有语句/分支覆盖度量 | ☐ |
| 基于经验的技术 | 可用于补充，**不得作为唯一覆盖依据** | ☐ |
| 技术选择留痕 | 每项技术写明用于哪些测试项与理由 | ☐ |

> **最常见的错误**：全部用例都用"错误推测"设计。这样写出来的用例看着很接地气，
> 但**覆盖率无法计算、无法证明测全了**，在准出评审上必然被退回。

---

## 5 测试用例标准卡片模板

**这是本仓库强制的用例格式。** 每条用例一张卡片，字段一个不能少。

```markdown
#### <TC-001> <用例标题：测试项 + 条件 + 预期，一句话说清>

| 字段 | 内容 |
|---|---|
| **用例编号** | `<TC-001>` |
| **对应用户需求编号** | `<REQ-F-001>`（多条用逗号分隔；**不得为空**） |
| **测试项** | `<所属测试项编号与名称，如 TI-02 金额校验>` |
| **测试级别** | `L0 骨架冒烟 / L1 单元 / L2 集成 / L3 系统 / L4 验收` |
| **测试类型** | `功能 / 性能 / 并发 / 异常 / 边界 / 恢复 / 安全 / 兼容` |
| **设计技术** | `等价类划分 / 边界值 / 判定表 / 状态迁移 / 场景法 / 错误推测` |
| **质量特性归属** | `<仅验收用例填写：GB/T 25000.51-2016 的 8 特性之一，如 功能性>` |
| **优先级** | `P0 / P1 / P2`（**P0 用例必须每次回归都跑**） |
| **前置条件** | `<环境、账号、权限、初始数据状态；必须具体到可复现>` |
| **测试数据** | `<具体值，不写"若干""正常数据"；引用数据脚本时写脚本与参数>` |
| **测试步骤** | `1. <动作> 2. <动作> 3. <动作>` |
| **预期结果** | `<具体到文案 / 状态码 / 数据变化 / 日志；禁止写"显示正确">` |
| **实际结果** | `<执行时填写>` |
| **判定** | `<通过 / 不通过 / 阻塞 / 未执行>` |
| **缺陷编号** | `<不通过时关联 BUG-001，通过时留空>` |
| **执行人 / 执行日期** | `<姓名>` / `<YYYY-MM-DD>` |
| **备注** | `<依赖、风险、复现线索、裁决说明>` |
```

### 5.1 用例设计的四条硬规则

1. **一条用例只验一个判定点。** 一条用例断言五个结果，失败时无法定位原因。
2. **预期结果必须可判定。** 写"提示"是错的，要写"提示『单笔金额不得超过 100000.00』且无订单产生且日志记录 WARN"。
3. **每条用例必须挂到至少一条需求编号。** 挂不上需求的用例要么需求漏了，要么用例多余——两种情况都要处理。
4. **用例必须可独立复现。** 执行者不看代码、不看作者、不看聊天记录，仅凭卡片即可执行。

---

## 6 示例用例（三条，按本模板填写）

### 示例一：正常流程

#### `<TC-001>` 订单提交——金额在合法区间内正常下单

| 字段 | 内容 |
|---|---|
| **用例编号** | `<TC-001>` |
| **对应用户需求编号** | `<REQ-F-001>`, `<REQ-F-012>` |
| **测试项** | `TI-01` 订单提交 |
| **测试级别** | `L3 系统测试` |
| **测试类型** | 功能测试 |
| **设计技术** | 场景法 + 等价类划分（有效等价类） |
| **质量特性归属** | 功能性 |
| **优先级** | `P0` |
| **前置条件** | 已登录普通用户；账户可用余额充足；系统单笔金额上限为 `100000.00`；订单表已重置为初始状态 |
| **测试数据** | 商品 `<SKU-001>` 单价 `99.00`，数量 `2`；订单金额 `198.00` |
| **测试步骤** | 1. 进入下单页，选择商品 `<SKU-001>`，数量填 `2`<br>2. 确认订单金额显示为 `198.00`<br>3. 点击「提交订单」 |
| **预期结果** | 1. 订单创建成功，返回订单号；订单状态为 `待支付`<br>2. 订单金额落库为 `198.00`，与页面显示一致<br>3. 接口返回 HTTP `200`，业务码 `0`<br>4. 日志记录 `INFO` 级订单创建事件，含订单号 |
| **实际结果** | `<执行时填写>` |
| **判定** | `<通过 / 不通过>` |
| **缺陷编号** | `<不通过时填 BUG-001>` |
| **执行人 / 执行日期** | `<姓名>` / `<YYYY-MM-DD>` |
| **备注** | 主流程冒烟用例，每次回归必跑 |

### 示例二：边界值

#### `<TC-B-001>` 订单提交——金额恰等于单笔上限

| 字段 | 内容 |
|---|---|
| **用例编号** | `<TC-B-001>` |
| **对应用户需求编号** | `<REQ-F-012>` |
| **测试项** | `TI-02` 金额校验 |
| **测试级别** | `L3 系统测试` |
| **测试类型** | 边界测试 |
| **设计技术** | 边界值分析（**上边界取值：恰好等于上限**） |
| **质量特性归属** | 功能性 |
| **优先级** | `P1` |
| **前置条件** | 已登录；账户可用余额 ≥ `100000.00`；单笔上限 `100000.00` |
| **测试数据** | 订单金额 = `100000.00`（**恰好等于上限，不超出**） |
| **测试步骤** | 1. 进入下单页，输入金额 `100000.00`<br>2. 点击「提交订单」 |
| **预期结果** | 1. **提交成功**（边界值应被接受，不得误拒）<br>2. 订单金额落库为 `100000.00`，精度无丢失<br>3. 接口返回业务码 `0` |
| **实际结果** | `<执行时填写>` |
| **判定** | `<通过 / 不通过>` |
| **缺陷编号** | `<不通过时填 BUG-001>` |
| **执行人 / 执行日期** | `<姓名>` / `<YYYY-MM-DD>` |
| **备注** | 与 `<TC-E-001>` 配对执行，共同验证边界判定的正确性与精度 |

### 示例三：异常路径

#### `<TC-E-001>` 订单提交——金额超出单笔上限被拒绝

| 字段 | 内容 |
|---|---|
| **用例编号** | `<TC-E-001>` |
| **对应用户需求编号** | `<REQ-F-012>` |
| **测试项** | `TI-02` 金额校验 |
| **测试级别** | `L3 系统测试` |
| **测试类型** | 异常测试（越界输入） |
| **设计技术** | 边界值分析（**上边界外侧：上限 + 最小精度单位**）+ 错误推测 |
| **质量特性归属** | 功能性 |
| **优先级** | `P0` |
| **前置条件** | 已登录；账户余额充足；单笔上限 `100000.00`；订单表已重置 |
| **测试数据** | 订单金额 = `100000.01`（上限 + `0.01`） |
| **测试步骤** | 1. 进入下单页，输入金额 `100000.01`<br>2. 点击「提交订单」<br>3. 查询订单表与接口响应 |
| **预期结果** | 1. 提交**被拒绝**，接口返回业务错误码 `<如 40001>`<br>2. 页面提示文案为「单笔金额不得超过 100000.00」<br>3. **订单表无新记录产生**（不得落库部分数据）<br>4. 日志记录 `WARN` 级校验失败事件，含尝试金额与用户标识<br>5. 账户余额**不发生任何变动** |
| **实际结果** | `<执行时填写>` |
| **判定** | `<通过 / 不通过>` |
| **缺陷编号** | `<不通过时填 BUG-001>` |
| **执行人 / 执行日期** | `<姓名>` / `<YYYY-MM-DD>` |
| **备注** | 若出现"提示拒绝但余额已扣减"→ 直接判定 **P0 致命**（数据一致性破坏） |

---

## 7 测试规程说明

**测试规程**（test procedure）定义用例的**执行顺序与编排**，解决"用例之间有依赖"的问题。

| 规程编号 | 规程名称 | 包含用例（有序） | 前置环境 | 数据重置方式 | 依赖说明 |
|---|---|---|---|---|---|
| `PROC-01` | 主流程冒烟规程 | `<TC-001>` → `<TC-002>` → `<TC-003>` | `ENV-L3` | `reset_orders.py` | 顺序执行，共享登录态 |
| `PROC-02` | 金额校验规程 | `<TC-B-001>` → `<TC-E-001>` | `ENV-L3` | `reset_orders.py --scope=amount` | 用例间**无依赖**，可乱序 |
| `PROC-03` | `<业务流程规程>` | `<TC-xxx>` → `<TC-yyy>` | `<环境>` | `<重置方式>` | `<依赖关系>` |

| 规程要素 | 要求 |
|---|---|
| 自动化映射 | 每条规程标注是否已自动化及对应测试标识（如 `pytest tests/test_order.py::test_submit`） |
| 停止条件 | 规程中出现 P0/P1 缺陷时**立即停止后续步骤**，登记缺陷并转阻塞判定 |
| 结果采集 | 自动化执行须留存日志与报告路径；手工执行须留存截图/日志片段 |
| 清理动作 | 规程结束后必须清理测试数据，避免污染后续规程 |

---

## 8 用例与需求追溯

### 8.1 正向追溯（需求 → 用例）

**每条已基线需求至少一条用例**，否则需求覆盖率不可能达到 100%。

| 需求编号 | 需求描述（摘要） | 对应用例编号 | 需求覆盖率 | 备注 |
|---|---|---|---|---|
| `<REQ-F-001>` | `<需求摘要>` | `<TC-001>` | ✅ | |
| `<REQ-F-012>` | `<金额上限校验>` | `<TC-001>`, `<TC-B-001>`, `<TC-E-001>` | ✅ | 含正常/边界/异常 |
| `<REQ-N-004>` | `<安全指标>` | `<TC-S-001>` | ✅ | |
| `<REQ-F-0xx>` | `<需求摘要>` | **（空）** | ❌ **缺口** | → 提 `<TBD-01>` 限期补齐 |

### 8.2 反向追溯（用例 → 需求）

| 用例编号 | 关联需求 | 是否可追溯 | 处置 |
|---|---|---|---|
| `<TC-001>` | `<REQ-F-001>` | ✅ | — |
| `<TC-0xx>` | **（空）** | ❌ | 要么补需求，要么删除用例——**不得保留无源用例** |

### 8.3 覆盖率核算

| 指标 | 计算式 | 目标 | 实际 | 判定 |
|---|---|---|---|---|
| 需求覆盖率 | 有对应用例的需求数 / 已基线需求总数 | **100%** | `<n>%` | ☐ |
| 用例设计完成率 | 已设计用例数 / 计划用例数 | `100%` | `<n>%` | ☐ |
| 四类路径覆盖 | 覆盖正常/边界/异常/空值的测试项数 / 测试项总数 | **100%** | `<n>%` | ☐ |

> 覆盖率由 `python tools/trace_matrix.py` 自动核算，**不采用人工统计**。
> CI 门禁在需求覆盖率 < 100% 时直接失败——这是机器能判的事，不交给人。

---

## 9 填写指引（常见错误）

1. **预期结果写"显示正确""功能正常"**——这是最致命的填写错误，等于没有预期结果。必须具体到**提示文案、业务码、数据库字段值、日志级别**中的至少两项，否则用例失败时无法判定是产品错了还是用例错了。
2. **测试数据写"若干""一批正常数据"**——测试数据必须写**具体值**。边界用例尤其如此：`100000.00` 与 `100000.01` 是两个完全不同的用例，不能含糊。
3. **用例没有需求编号（或事后补挂）**——需求覆盖率会因此无法达到 100%。**先写需求编号再写用例**，挂不上编号时先解决需求问题，不要先写用例再说。
4. **把多条判定点塞进一条用例**——"提交订单并检查库存、余额、短信、日志"是四条用例。一条用例多个判定点，失败后定位成本成倍增加。
5. **测试项只测正常路径**——正常、边界、异常、**空值**四类必须都有。空值（`null` / 空字符串 / `0` / 空集合）是最常被漏测且最容易崩溃的一类。
6. **用例编号复用**——作废用例的编号不得回收给新用例，否则历史测试报告中的编号将指向错误的用例含义，追溯体系失效。

---

## 附：本模板的保守处理声明

| 项 | 保守处理方式 | 原因 |
|---|---|---|
| 国标条款编号 | 不引用具体条款号，按文档内容要求组织章节 | 条款编号须以标准正文为准 |
| 测试设计技术分类 | 只列出本项目实际可落地的 6 种技术，并标注适用/不适用场景 | GB/T 38634.4-2020 技术种类多于此处列举，**完整清单请查标准正文**；本表为落地子集 |
| 质量特性归属 | 验收用例按 GB/T 25000.51-2016 的 8 个特性归类，**不细分到子特性编号** | 子特性编号体系以标准正文为准；避免自行编号造成误导 |
| GB/T 15532-2008 | 明确标注为老标准，不作主依据 | 防止新项目引用过时标准 |
