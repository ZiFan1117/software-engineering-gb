# 骨架跑通验证记录

> **文档编号**：`<项目代号>-SK-<序号>-V<主版本>.<次版本>`
> **编号规则**：`<项目代号>-<类别码>-<三位序号>-V<主版本>.<次版本>`；本文件类别码 **SK**（SKeleton verification），
> 兼作 **R3 骨架跑通评审**的评审材料。
> **适用阶段**：**S3 骨架跑通**（S3 的核心交付物；R3 评审通过后方可进入 S4 模块实现）。
> **依据标准**：
> - **GB/T 38634.2-2020**《系统与软件工程 软件测试 第2部分：测试过程》——冒烟测试作为动态测试过程的前置验证；
> - **GB/T 38634.3-2020**《第3部分：测试文档》——测试记录类文档的内容要求；
> - **GB/T 38634.4-2020**《第4部分：测试技术》——冒烟用例的设计技术选择；
> - GB/T 8566-2022《系统与软件工程 软件生存周期过程》——实现过程的前置验证；
> - GB/T 8567-2006《计算机软件文档编制规范》——文档编写要求。
>
> **状态**：**模板**（未填写）。填写后随框架基线 `framework/v0.x` 归档；**每次框架结构性变更后须重填**。
>
> **说明**：本模板不复制国标正文。**GB/T 15532-2008 是 2008 年老标准，新项目应以 GB/T 38634 系列为准**。

---

## 1 目的与定位

### 1.1 为什么要单独做「骨架跑通」

> **骨架跑通评审（R3）的目的，是用最低成本暴露框架的结构性错误。**
>
> 此时代码量最小、依赖最少、推翻框架的代价最低。装配方式错了、模块边界切错了、
> 依赖成环了、构建脚本缺了——这些问题在 S3 被发现，改的是**框架**；
> 拖到 S5 集成时才被发现，改的是**框架 + 所有已建立在该框架假设之上的模块 + 全部相关测试**。
> 两者的修复成本不在一个量级。

| 对照项 | S3 骨架跑通时发现 | S5 集成测试时发现 |
|---|---|---|
| 已实现模块数 | 0（全部为桩/Mock） | 全部 |
| 需重写的代码 | 框架装配代码 | 框架 + 各模块适配代码 |
| 需重跑的设计文档 | HLD、模块清单、契约 | 同上 + 各模块详细设计 |
| 对进度的影响 | 天级 | 周级 |
| 对团队信心的影响 | 低（本来就是骨架） | 高（"都快做完了才发现"） |

### 1.2 本记录要证明什么

| 编号 | 待证明事项 | 对应 R3 准出项 |
|---|---|---|
| `P-01` | **一条命令**能在干净环境跑通骨架 | 一条命令可跑通 |
| `P-02` | 全部模块以桩/Mock 形式装配成功 | 模块装配成功 |
| `P-03` | 模块注册与依赖解析正确，**无环** | 无循环依赖 |
| `P-04` | 冒烟测试用例存在且通过 | 冒烟测试通过 |
| `P-05` | CI 在干净环境可复现通过 | CI 可复现 |
| `P-06` | 日志、配置加载路径已验证 | 可观测性与配置可用 |

### 1.3 标识

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-SK-<序号>-V<主>.<次>` |
| 骨架版本 / 提交号 | `<commit-hash>`；框架基线 `<framework/v0.x>` |
| 验证环境 | `<OS + 版本>` / `<运行时 + 精确版本>` |
| 验证人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审类型 | **R3 骨架跑通评审**（主持：技术负责人；必参：开发、测试） |

---

## 2 验证对象与前置条件

### 2.1 验证对象

| 对象 | 路径/标识 | 说明 |
|---|---|---|
| 骨架入口 | `<如 skeleton/run_skeleton.py>` | 一条命令的入口 |
| 构建/运行脚本 | `<如 build.ps1 / Makefile>` | 无脚本则记 `TBD-01` |
| 模块注册机制 | `<如 registry / 插件声明 / 配置文件>` | 装配的核心 |
| 配置加载 | `<如 config/*.toml>` | 配置路径验证对象 |
| 日志初始化 | `<如 logging 初始化模块>` | 可观测性验证对象 |
| 桩/Mock 模块 | `<路径或目录>` | 全部未实现模块的占位 |
| CI 流水线定义 | `<如 .github/workflows/ci.yml>` | 干净环境复现依据 |

### 2.2 前置条件核对

| 前置条件 | 要求 | 实际 | 判定 |
|---|---|---|---|
| 框架基线已建立 | 存在标签 `framework/v0.x` | `<标签>` | ☐ |
| HLD 与模块清单已获批 | R2 框架评审通过 | `<评审记录编号>` | ☐ |
| 模块接口契约已定义 | 每个模块有对外契约 | ☐ 齐备 ☐ 缺 `<Mnn>` | ☐ |
| 骨架代码已提交 | 含构建脚本 | `<commit-hash>` | ☐ |
| 干净环境可用 | 无残留产物、无手工改动 | ☐ | ☐ |

---

## 3 一条命令跑通记录（**核心证据，留空待填**）

### 3.1 执行命令

> **要求：一条命令。** 若需要先执行三步准备、再执行主命令，则骨架**未达 R3 标准**——
> 准备步骤必须收敛进脚本。多命令跑通不算跑通，因为它无法在 CI 中可靠复现。

```powershell
# 在干净环境中，仅执行以下一条命令：
<如：python skeleton/run_skeleton.py>
```

| 项 | 内容 |
|---|---|
| 工作目录 | `<仓库根目录 / 其他>` |
| 命令 | `<一条完整命令，含必要参数>` |
| 前置手工步骤 | **应为"无"**；若有，逐条列出并说明为何未收敛进脚本 |
| 预期结果 | 退出码 `0`，输出骨架自检/模块注册/依赖拓扑/模块号生成结果 |

### 3.2 实际输出记录（**留空待填**）

> 粘贴**完整原始输出**，不得摘要、不得删改。输出过长时可附完整日志文件路径，
> 但关键片段（自检结果、注册数量、依赖拓扑、退出码）必须内联展示。

```text
<在此粘贴实际执行输出。占位示例结构：>

=== 骨架自检 ===
[ ] 配置加载 ......... <结果>
[ ] 日志初始化 ....... <结果>
[ ] 模块注册 ......... <已注册模块数>
[ ] 依赖拓扑 ......... <无环 / 存在环>
[ ] 模块号生成 ....... <结果>

=== 冒烟结果 ===
<通过/失败 汇总>

=== 退出码 ===
<0>
```

| 核验项 | 预期 | 实际 | 判定 |
|---|---|---|---|
| 退出码 | `0` | `<实际值>` | ☐ |
| 配置加载成功 | 是 | `<是/否>` | ☐ |
| 日志初始化成功 | 是 | `<是/否>` | ☐ |
| 全部模块注册成功 | 是（`<n>` 个） | `<n>` 个 | ☐ |
| 依赖解析无环 | 是 | `<是/否>` | ☐ |
| 输出无未捕获异常 | 是 | `<是/否>` | ☐ |
| 输出无 `ERROR` 级日志 | 是 | `<是/否>` | ☐ |

### 3.3 输出摘要（人对输出的判断）

```
<用 2–4 句话说明：命令做了什么、关键输出说明什么、有无异常/警告、
 若有警告为何可接受或为何不可接受。>
```

---

## 4 模块装配清单核对（**核心证据**）

### 4.1 装配方式说明

| 装配方式 | 含义 | 本阶段是否允许 |
|---|---|---|
| **桩（Stub）** | 返回固定值的占位实现，用于打通装配 | ✅ **S3 的标准方式** |
| **Mock** | 可编程的假实现，可断言调用行为 | ✅ 允许 |
| **真实实现** | 已完成业务逻辑的模块 | ⚠️ S3 阶段应尽量少；若已有（如公共工具模块），须标注 |
| **未装配** | 未接入框架 | ❌ **不允许**——缺失装配即为 R3 不通过 |

### 4.2 模块装配核对表

> **逐一核对，不得抽样。** 每个模块号都必须出现，与模块清单登记表一一对应。

| 模块号 | 模块名称 | 对应需求 | 装配方式 | 注册结果 | 契约加载 | 依赖声明 | 判定 | 备注 |
|---|---|---|---|---|---|---|---|---|
| `<M01>` | `<模块名称>` | `<REQ-F-001>` | 桩 / Mock / 真实 | ☐ 成功 ☐ 失败 | ☐ 成功 | `<依赖模块号或"无">` | ☐ | |
| `<M02>` | `<模块名称>` | `<REQ-F-005>` | 桩 | ☐ 成功 ☐ 失败 | ☐ 成功 | `<M01>` | ☐ | |
| `<M03>` | `<模块名称>` | `<REQ-N-004>` | 桩 | ☐ 成功 ☐ 失败 | ☐ 成功 | `<M01>, <M02>` | ☐ | |
| `<Mnn>` | `<模块名称>` | `<需求>` | `<方式>` | ☐ | ☐ | `<依赖>` | ☐ | |

### 4.3 装配一致性核对

| 核对项 | 要求 | 实际 | 判定 |
|---|---|---|---|
| 模块清单中登记模块数 | `<n>` | — | — |
| 实际注册成功模块数 | `<n>`（必须等于登记数） | `<n>` | ☐ |
| 未装配模块数 | **0** | `<n>` | ☐ |
| 模块号命名合规 | 符合 `M` + 2 位规则 | ☐ | ☐ |
| 模块号无重复 | 登记表与实际一致 | ☐ | ☐ |
| 模块职责与契约一致 | 每模块有明确对外契约 | ☐ | ☐ |
| 桩实现未混入业务逻辑 | 桩只做占位，不承载真实规则 | ☐ | ☐ |

> **常见陷阱**：桩实现里"顺手"写了点业务逻辑（"反正是个默认值"）。
> 这会让骨架跑通产生**虚假信心**——真实的模块接入后行为不一致，S4 才发现。
> 桩只返回固定值或约定的假数据，**不实现任何业务规则**。

---

## 5 依赖解析无环验证（**核心证据**）

### 5.1 依赖拓扑记录

> **模块间循环依赖是框架级结构缺陷**，必须在 S3 消灭。
> 它在单模块开发时毫无症状，在集成时表现为初始化顺序无法确定、测试无法独立运行。

```text
<粘贴依赖拓扑输出，或手工绘制的依赖关系。示例结构：>

<M01>
 ├─→ <M02>
 │    ├─→ <M03>
 │    └─→ <M04>
 └─→ <M05>

拓扑排序结果：<M01> → <M02> → <M03> → <M04> → <M05>
环检测结果：无环 / 存在环：<列出环路>
```

### 5.2 环检测核对

| 检查项 | 方法 | 结果 | 判定 |
|---|---|---|---|
| 全图环检测 | `<拓扑排序 / DFS 检测 / 工具>` | ☐ 无环 ☐ 存在环 | ☐ |
| 跨层依赖合规 | 上层可依赖下层，下层不得反向依赖上层 | ☐ 合规 | ☐ |
| 依赖声明完整性 | 每个模块声明的依赖与其实际使用一致 | ☐ 一致 | ☐ |
| 无隐式依赖 | 无"通过全局变量/单例偷偷拿到"的依赖 | ☐ | ☐ |
| 拓扑排序可生成 | 存在至少一个合法的初始化顺序 | ☐ | ☐ |
| 模块可独立测试 | 每个模块可在不启动全系统的前提下被测试 | ☐ | ☐ |

### 5.3 若发现环（处置记录）

| 环路 | 涉及模块 | 环的成因 | 处置方案 | 状态 |
|---|---|---|---|---|
| `<M02> → <M05> → <M02>` | `<M02>, <M05>` | `<职责错位 / 契约不足>` | `<提 R5 框架变更 / 抽取公共模块>` | ☐ 已消除 |
| — | — | **（无环时填写"无"）** | — | — |

> **若存在未消除的环，R3 判定为不通过**，并需按 **FC-1 契约不足**或 **FC-2 职责错位**
> 提起框架变更（R5），回到 S2 调整模块划分后重新验证。

---

## 6 冒烟测试用例与结果

### 6.1 冒烟用例设计原则

| 原则 | 说明 |
|---|---|
| **数量少** | 通常 `<5>`–`<15>` 条；冒烟是"能不能跑"，不是"跑得对不对" |
| **覆盖装配** | 至少一条用例触达每个模块的装配入口 |
| **覆盖主链路** | 至少一条用例跑通端到端主流程（全部由桩支撑） |
| **执行快** | 总执行时间 `<n>` 秒级；分钟级说明设计过重 |
| **可自动化** | 必须能在 CI 中自动执行并给出明确退出码 |

### 6.2 冒烟用例清单

| 用例编号 | 用例名称 | 验证目标 | 对应事项 | 级别 | 预期结果 | 实际结果 | 判定 |
|---|---|---|---|---|---|---|---|
| `<TC-001>` | 骨架启动自检 | 配置加载 + 日志初始化 | `P-01`, `P-06` | L0 | 退出码 0，输出自检结果 | `<待填>` | ☐ |
| `<TC-002>` | 模块全量注册 | 全部模块可被注册 | `P-02` | L0 | 注册数 = `<n>`，无失败项 | `<待填>` | ☐ |
| `<TC-003>` | 依赖拓扑生成 | 依赖解析无环 | `P-03` | L0 | 拓扑排序成功，无环 | `<待填>` | ☐ |
| `<TC-004>` | 主链路端到端贯通 | 请求经全部模块（桩）返回结果 | `P-02`, `P-04` | L0 | 主链路返回预期结构的数据 | `<待填>` | ☐ |
| `<TC-005>` | 配置项缺失的受控失败 | 配置缺失时给出可读错误而非崩溃 | `P-06` | L0 | 明确错误信息 + 非零退出码 | `<待填>` | ☐ |
| `<TC-00n>` | `<用例名称>` | `<目标>` | `<事项>` | L0 | `<预期>` | `<待填>` | ☐ |

### 6.3 冒烟结果统计

| 指标 | 数值 | 要求 | 判定 |
|---|---|---|---|
| 冒烟用例总数 | `<n>` | — | — |
| 通过 | `<n>` | 全部通过 | ☐ |
| 失败 | `<n>` | **0** | ☐ |
| 阻塞 | `<n>` | **0** | ☐ |
| 总执行时间 | `<n>` 秒 | 秒级 | ☐ |
| 是否可自动化执行 | ☐ 是 ☐ 否 | **是** | ☐ |

---

## 7 CI 干净环境复现记录

> **本地跑通 ≠ 跑通。** 本地环境可能有未提交的改动、手工安装的依赖、残留的构建产物。
> **只有在干净环境中复现通过，骨架才算真的跑通。**

### 7.1 干净环境定义

| 要素 | 要求 | 实际 |
|---|---|---|
| 环境来源 | 全新检出（fresh clone）/ 全新容器 / 干净虚拟机 | `<方式>` |
| 手工前置操作 | **无**（不得手工装依赖、改配置、建目录） | ☐ 无 ☐ 有：`<列出>` |
| 依赖获取 | 由锁定文件自动安装 | ☐ |
| 构建产物 | 从零生成，无复用 | ☐ |
| 环境变量 | 仅使用文档中声明的变量 | ☐ |
| 密钥/凭据 | 不依赖任何真实密钥（用测试占位） | ☐ |

### 7.2 复现记录

| 项 | 内容 |
|---|---|
| CI 平台与流水线 | `<平台> / <流水线名称>` |
| 触发方式 | `<push / pull_request / 手动>` |
| 运行编号 | `<run-id>` |
| 运行时间 | `<YYYY-MM-DD HH:MM>` |
| 结果 | ☐ 通过 ☐ 失败 |
| 耗时 | `<n>` 秒/分 |
| 日志路径 | `<链接或路径>` |

### 7.3 CI 步骤核对

| 步骤 | 是否包含 | 结果 | 备注 |
|---|---|---|---|
| 干净检出代码 | ☐ | ☐ | |
| 按锁定文件安装依赖 | ☐ | ☐ | |
| 执行构建脚本 | ☐ | ☐ | |
| **执行骨架跑通命令** | ☐ | ☐ | 本记录第 3 章的同一条命令 |
| 执行冒烟测试 | ☐ | ☐ | |
| 执行单元测试（若有） | ☐ | ☐ | |
| 归档日志与产物 | ☐ | ☐ | |

### 7.4 本地与 CI 结果差异分析

| 项 | 本地结果 | CI 结果 | 是否一致 | 差异原因 |
|---|---|---|---|---|
| 骨架跑通 | `<通过/失败>` | `<通过/失败>` | ☐ | `<原因>` |
| 冒烟测试 | `<通过/失败>` | `<通过/失败>` | ☐ | `<原因>` |

> **差异必须被解释。** "本地能跑 CI 不能跑"通常意味着存在未提交的依赖、隐式的环境变量
> 或平台相关假设——这些都是 S3 必须暴露的结构性问题，不得以"CI 环境问题"一笔带过。

---

## 8 框架假设的初步验证

> S2 产出的**框架假设清单**中标注"验证时点 = S3 骨架跑通"的条目，在此给出结论。

| 假设编号 | 假设内容 | 验证方式 | 结论 | 若不成立的影响 | 后续动作 |
|---|---|---|---|---|---|
| `A-01` | `<如：上游数据源为同步阻塞调用>` | `<骨架中以桩验证调用形态>` | ☐ 成立 ☐ 推翻 | `<影响>` | `<触发 FC-3 / 无需动作>` |
| `A-02` | `<假设内容>` | `<方式>` | ☐ 成立 ☐ 推翻 | `<影响>` | `<动作>` |

| 统计 | 数值 |
|---|---|
| 本次验证的假设数 | `<n>` |
| 成立 | `<n>` |
| **被推翻（需提框架变更）** | `<n>` |

> 被推翻的假设必须按 **FC-3 假设被推翻**提起框架变更评审（R5），并复盘"假设为何错"。

---

## 9 问题与处置

| 序号 | 问题描述 | 类别 | 严重程度 | 处置措施 | 责任人 | 期限 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | `<如：M05 注册顺序依赖 M02 的副作用>` | 装配问题 | 高 | `<收敛到显式依赖声明>` | `<姓名>` | `<YYYY-MM-DD>` | ☐ |
| 2 | `<如：配置缺失时抛未捕获异常>` | 健壮性 | 中 | `<改为可读错误>` | `<姓名>` | `<YYYY-MM-DD>` | ☐ |
| 3 | `<如：构建脚本未处理依赖安装>` | 可复现性 | 高 | `<补入脚本>` | `<姓名>` | `<YYYY-MM-DD>` | ☐ |
| 4 | `<TBD-01>` | `<类别>` | `<程度>` | `<措施>` | `<姓名>` | `<YYYY-MM-DD>` | ☐ |

---

## 10 结论

### 10.1 R3 准出核对

| 序号 | R3 准出项 | 判据 | 实际 | 判定 |
|---|---|---|---|---|
| 1 | **一条命令**能在干净环境跑通骨架 | 退出码 0，无手工前置步骤 | `<实际>` | ☐ |
| 2 | 全部模块以桩/Mock 形式装配成功 | 注册数 = 模块清单登记数，未装配数 = 0 | `<n>/<n>` | ☐ |
| 3 | 模块注册与依赖解析正确，无环 | 环检测无环，拓扑排序可生成 | `<结果>` | ☐ |
| 4 | 冒烟测试用例存在且通过 | 通过率 100% | `<n>%` | ☐ |
| 5 | CI 流水线在干净环境可复现通过 | CI 运行通过 | `<结果>` | ☐ |
| 6 | 日志、配置加载路径已验证 | 日志可输出、配置可加载、缺失时可读报错 | `<结果>` | ☐ |

### 10.2 结论

| 项 | 内容 |
|---|---|
| 通过项 | `<n>` / `6` |
| 框架假设被推翻数 | `<n>`（需提 R5） |
| 未消除的循环依赖 | `<n>`（**必须为 0**） |
| **验证结论** | ☐ **通过**（可进入 S4 模块实现）　☐ **不通过**（退回修改后重新验证） |
| 若通过，框架基线 | `framework/v0.x` ☐ 已打标签 ☐ 已冻结 |
| 若不通过，退回原因 | `<明确写清哪项未满足、需完成哪些动作>` |
| 后续动作 | ☐ 无 ☐ 提框架变更（`FC-<年>-<序号>`） ☐ 补 `<文档/脚本>` |

### 10.3 签字栏（**R3 骨架跑通评审**）

| 角色 | 姓名 | 结论（通过/不通过） | 签字 | 日期 |
|---|---|---|---|---|
| 开发（验证执行人） | `<姓名>` | ☐ 通过 ☐ 不通过 | | `<YYYY-MM-DD>` |
| 测试 | `<姓名>` | ☐ 通过 ☐ 不通过 | | `<YYYY-MM-DD>` |
| **技术负责人（主持）** | `<姓名>` | ☐ 通过 ☐ 不通过 | | `<YYYY-MM-DD>` |

> R3 **可简化流程，但必须执行**。没有 R3 结论，不得进入 S4 模块实现。
> **口头通过等于没通过。**

---

## 11 填写指引（常见错误）

1. **只填"跑通了"，不贴原始输出**——第 3.2 节必须粘贴**完整原始输出**。摘录关键几行无法证明"没有隐藏的异常与错误日志"，评审时不予采信。
2. **用多条命令或手工前置步骤凑出"跑通"**——先装依赖、再改配置、再建目录、最后跑主命令，这不叫一条命令跑通。所有前置步骤必须收敛进脚本，否则 CI 无法可靠复现。
3. **模块装配表抽样填写**——必须**逐一核对全部模块号**，并与模块清单登记表对齐。漏填的模块按"未装配"处理，直接导致 R3 不通过。
4. **桩实现里写了业务逻辑**——桩只返回固定值或约定假数据。桩里塞业务规则会让骨架跑通产生虚假信心，真实模块接入后行为不一致，问题被推迟到 S4/S5 才暴露。
5. **本地跑通即认定通过，不验 CI**——本地环境常有未提交改动与手工安装的依赖。"CI 跑不过"必须查明并修复或解释，不得以"CI 环境问题"跳过。
6. **发现循环依赖后带着问题通过**——未消除的环是**框架级结构缺陷**，必须提 R5 框架变更（FC-1/FC-2）回到 S2 调整划分后重新验证，不得在 R3 结论中标注"后续处理"而放行。

---

## 附：本模板的保守处理声明

| 项 | 保守处理方式 | 原因 |
|---|---|---|
| 国标条款编号 | 不引用具体条款号，按测试记录类文档的内容要求组织章节 | 条款编号须以标准正文为准 |
| **S3 阶段与 R3 评审** | 明确声明为**本仓库的工程约定与强化项**，不声称是国标规定的阶段或评审类型 | GB/T 8566-2022 给过程框架、GB/T 38634 系列给测试过程与文档，二者均**未规定"S3 骨架跑通"这一独立阶段**；本文件是其落地裁剪 |
| 冒烟测试的国标定位 | 表述为"对齐 GB/T 38634.2-2020 动态测试过程的**前置验证**"，不指定具体过程名称 | 避免把本仓库约定直接等价为标准的某个特定过程 |
| 具体命令与输出 | 全部留空为占位符，**不预填任何示例执行结果** | 防止示例被误当作真实验证证据 |
| GB/T 15532-2008 | 明确标注为老标准，不作主依据 | 防止新项目引用过时标准 |
