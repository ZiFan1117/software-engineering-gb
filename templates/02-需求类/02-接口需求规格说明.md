# 接口需求规格说明（IRS）

> **文档编号规则**：`<项目代号>-IRS-<顺序号>-v<主版本>.<次版本>`，示例 `<项目代号>-IRS-001-v1.0`（规则见 §0.2）
> **适用阶段**：**S1 需求工程**（与 `01-软件需求规格说明.md` 同期编写，同为 **R1 需求评审**的准出材料）
> **依据标准**：**GB/T 8567-2006**《计算机软件文档编制规范》（接口需求规格说明文档种类）；需求条目依据 GB/T 9385-2008；过程依据 GB/T 8566-2022
> **文档状态**：**模板（Template）** · 未基线化 · 禁止未经填写与评审直接作为交付物提交

---

## 零、使用说明（填完全文后请删除本章）

### 0.1 本模板解决什么问题

接口是**最容易在集成阶段爆炸**的地方。原因几乎总是同一个：**接口只在"成功路径"上被定义过。**

本模板要求每个接口都必须写清 8 件事：**编号 · 类型 · 协议 · 数据格式 · 字段表 · 错误码表 · 幂等性 · 超时与重试 · 版本兼容策略**。
少写任何一项，S5 集成测试就会替你补课。

### 0.2 编号规则

| 对象 | 编号格式 | 示例 | 说明 |
|---|---|---|---|
| 本文档 | `<项目代号>-IRS-<顺序号>-v<主版本>.<次版本>` | `PRJ-IRS-001-v1.0` | |
| 接口 | `IF-<3 位序号>` | `<IF-001>` | 一经分配**永不复用**；接口废弃后编号作废不回收 |
| 接口字段 | `<接口编号>-F<2 位序号>` | `IF-001-F03` | 用于测试用例与问题单精确定位字段 |
| 错误码 | `<域前缀>-<4 位数字>` | `AUTH-4011` | 域前缀 2–6 位大写字母；全项目唯一，不得跨域复用 |
| 接口需求 | `IF-REQ-<2 位序号>` | `IF-REQ-01` | 适用于全部接口的通则 |
| 设计模块 | `M<2 位序号>` | `<M01>` | 见 `../03-设计类/02-模块清单与模块号登记表.md` |

### 0.3 占位符约定

| 占位符 | 含义 |
|---|---|
| `<项目名称>` / `<项目代号>` | 项目全称 / 文档编号短代号 |
| `<IF-001>` | 接口编号 |
| `<M01>` | 提供或消费该接口的模块号 |
| `<REQ-F-001>` / `<REQ-N-001>` | 该接口支撑的功能 / 非功能需求编号 |
| `<TBD-01>` | 待决事项（须有责任人与关闭日期） |
| `<YYYY-MM-DD>` / `<姓名>` | 日期 / 人名 |

### 0.4 文档信息

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-IRS-001-v1.0` |
| 版本号 | v1.0 |
| 编写人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审人 / 日期 | `<姓名>` / `<YYYY-MM-DD>`（R1 需求评审） |
| 批准人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 关联文档 | 软件需求规格说明 `<项目代号>-SRS-001`；需求追溯矩阵 `<项目代号>-RTM-001` |
| 接口总数 | 外部 `<n>` 个 · 内部 `<n>` 个 |

### 0.5 修订记录

| 版本 | 日期 | 修订人 | 修订类型 | 修订内容摘要 | 关联变更单 | 基线标签 |
|---|---|---|---|---|---|---|
| v0.1 | `<YYYY-MM-DD>` | `<姓名>` | 新建 | 初稿 | — | — |
| v1.0 | `<YYYY-MM-DD>` | `<姓名>` | 基线 | R1 评审后基线化 | — | `req-baseline/v1.0` |
| | | | | | | |

---

## 1 引言

### 1.1 编写目的

定义 `<项目名称>` 全部外部接口与关键内部接口的需求，作为接口设计、实现、联调、集成测试的**唯一基准**。

- **预期读者**：架构师、开发、测试、第三方系统对接人、运维；
- **用途**：接口基线化后，任何接口变更（含字段增减、错误码调整、超时调整）**必须走变更控制流程**，并同步更新本文档与需求追溯矩阵。

### 1.2 范围

| 项 | 内容 |
|---|---|
| 覆盖的接口类型 | 外部系统接口 / 系统与模块间内部接口 / 用户接口 |
| 覆盖的交互方式 | 同步调用（REST/RPC/SQL）、异步消息（MQ/事件）、文件交换（批量导入导出） |
| **不在范围内** | 例如：第三方系统**内部**实现细节、纯前端组件间通信、调试用临时接口 |
| 接口数量总览 | 外部 `<n>` 个（`<IF-001>`–`<IF-0nn>`）；内部 `<n>` 个 |

### 1.3 术语、定义与缩略语

| 术语 / 缩略语 | 全称 | 定义 |
|---|---|---|
| 幂等性 | Idempotency | 同一请求重复执行 N 次，对系统状态的影响与执行 1 次相同 |
| TPS | Transactions Per Second | 每秒事务数 |
| P95 / P99 | 95th / 99th Percentile | 响应时间分位值 |
| 向后兼容 | Backward Compatibility | 新版本接口能接受旧版本客户端的请求 |
| 契约 | Contract | 见 `../03-设计类/03-模块接口契约.md`：框架与模块之间的唯一法定接口 |
| `<术语>` | | |

### 1.4 参考资料

| 编号 | 名称 | 编号/版本 | 来源 |
|---|---|---|---|
| REF-01 | `<项目名称>` 软件需求规格说明 | `<项目代号>-SRS-001-v1.0` | 本项目 |
| REF-02 | GB/T 8567-2006 计算机软件文档编制规范 | — | 国家标准 |
| REF-03 | GB/T 9385-2008 计算机软件需求规格说明规范 | — | 国家标准 |
| REF-04 | `<第三方>` 接口技术文档 | `<版本>` | 第三方 |
| REF-05 | `<项目名称>` 模块接口契约 | `<项目代号>-IC-001` | 本项目（S2 产出，内部接口的落地形态） |

---

## 2 接口通则需求（适用于全部接口）

| 编号 | 类别 | 需求内容 | 验证方式 |
|---|---|---|---|
| `IF-REQ-01` | 定义完整性 | 每个接口必须定义：编号、名称、类型、协议、数据格式、字段表、错误码表、幂等性、超时与重试、版本兼容策略 | 本文档自查表 |
| `IF-REQ-02` | 错误码 | 所有错误必须返回**业务错误码 + 可读消息**，禁止只返回 500 与堆栈 | 集成用例 |
| `IF-REQ-03` | 幂等 | 所有**写类**接口必须声明幂等性；非幂等接口必须提供幂等键（`Idempotency-Key`） | 集成用例（重复提交） |
| `IF-REQ-04` | 超时 | 每个接口必须定义**调用方超时**与**服务方处理上限**，且服务方上限 < 调用方超时 | 设计核查 |
| `IF-REQ-05` | 重试 | 重试策略必须显式声明（次数、间隔、退避），且**重试必须幂等** | 集成用例（注入超时） |
| `IF-REQ-06` | 兼容 | 接口变更必须声明兼容级别：兼容扩展 / 破坏性变更（需并行期与弃用通告） | 变更评审（R5） |
| `IF-REQ-07` | 安全 | 所有跨信任域接口必须鉴权与加密（HTTPS/TLS ≥ 1.2）；敏感字段传输与日志中必须脱敏 | 安全用例 |
| `IF-REQ-08` | 可观测 | 每个接口必须有唯一 `trace_id` 贯穿调用链，日志可关联 | 日志抽样 |
| `IF-REQ-09` | 限流 | 对外接口必须定义限流阈值与超限行为（`429` + `Retry-After`） | 压测用例 |
| `IF-REQ-10` | 时间与编码 | 时间统一 `ISO 8601` + 时区（如 `2026-02-14T10:00:00+08:00`）；字符集统一 UTF-8 | 契约测试 |

---

## 3 接口清单与详细定义

### 3.1 接口清单表

> **这是全项目的接口总账。** 新增接口必须先在此表登记编号，再写详细定义。
> 接口编号一经分配**永不复用**；废弃接口保留行并把「状态」置为「已废弃」，注明废弃日期与替代接口。

| 接口编号 | 接口名称 | 类型 | 方向 | 提供方 | 消费方 | 协议 | 数据格式 | 同步/异步 | 频率/吞吐 | 支撑需求 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `<IF-001>` | 统一认证登录 | 外部 | 入 | `<外部系统 A>` | `<M20>`（经 `<M60>` 适配） | HTTPS/REST | JSON | 同步 | 峰值 50 TPS | `<REQ-F-001>` | 已定义 |
| `<IF-002>` | 用户注销 | 外部 | 入 | `<外部系统 A>` | `<M20>`（经 `<M60>` 适配） | HTTPS/REST | JSON | 同步 | 峰值 20 TPS | `<REQ-F-002>` | 已定义 |
| `<IF-003>` | 任务仓储查询 | 内部 | 入 | `<M40>` | `<M20>` | 进程内调用 | 结构体 | 同步 | 峰值 500 QPS | `<REQ-N-001>` | 已定义 |
| `<IF-004>` | 登录页提交 | 内部 | 入 | `<M01>` | `<M20>` | 进程内调用 | 结构体 | 同步 | 峰值 100 QPS | `<REQ-F-001>` | 已定义 |
| `<IF-005>` | 认证服务适配 | 内部 | 入 | `<M60>` | `<M20>` | 进程内调用 | 结构体 | 同步 | 峰值 50 QPS | `<REQ-F-001>` | 已定义 |
| `<IF-007>` | 任务状态变更通知 | 外部 | 出 | `<M20>` | `<外部系统 C>` | MQ | JSON | 异步 | 日均 20 万条 | `<REQ-F-030>` | 已定义 |
| `<IF-020>` | `<接口名称>` | | | `<M01>` | `<M02>` | | | | | | 设计中 |
| `<IF-021>` | `<接口名称>` | | | | | | | | | | 已废弃（`<YYYY-MM-DD>`，替代 `<IF-020>`） |

**方向说明**：入 = 本系统调用对端；出 = 对端调用本系统；双向 = 回调 + 回执。

### 3.2 接口详细定义模板

> **复制本节，每个接口一节。** 编号从 §3.2.1 递增，节标题与接口清单表的编号严格对应。

#### 3.2.1 `<IF-001>` 统一认证登录

##### 3.2.1.1 基本信息

| 项 | 内容 |
|---|---|
| **接口编号** | `<IF-001>` |
| **接口名称** | 统一认证登录 |
| **类型** | 外部接口（同步） |
| **方向** | 入（本系统调用统一认证平台） |
| **提供方 / 消费方** | `<外部系统 A>` / `<M20>`（业务层-认证服务） |
| **协议** | HTTPS / REST（`POST /api/v1/auth/login`） |
| **数据格式** | JSON（`Content-Type: application/json; charset=utf-8`） |
| **认证方式** | 服务间：双向 TLS + 应用凭证（`X-App-Id` + `X-App-Secret`）；本接口不涉及终端用户令牌 |
| **版本策略** | URL 版本号 `/v1/`；当前 v1，见 §3.2.1.8 |
| **支撑需求** | `<REQ-F-001>`、`<REQ-N-013>` |
| **关联设计模块** | `<M20>`（消费）、`<M60>`（适配层封装） |
| **状态** | 已定义 / 已确认（`<YYYY-MM-DD>`，与 `<第三方>` 联调确认） |

##### 3.2.1.2 请求定义

| 字段编号 | 字段名 | 类型 | 必填 | 取值范围/长度 | 说明 | 脱敏 |
|---|---|---|---|---|---|---|
| `IF-001-F01` | `account` | string | 是 | 6–32，`^[A-Za-z0-9_]+$` | 用户账号 | 否 |
| `IF-001-F02` | `password` | string | 是 | 8–64 | 用户口令（明文传输，靠 TLS 保护） | **是（日志掩码）** |
| `IF-001-F03` | `client_ip` | string | 否 | IPv4/IPv6 | 终端 IP，用于风控 | 否 |
| `IF-001-F04` | `captcha_token` | string | 否 | ≤ 256 | 连续失败 3 次后必填 | 否 |

**请求示例**

```json
{
  "account": "zhangsan",
  "password": "********",
  "client_ip": "10.20.30.40"
}
```

##### 3.2.1.3 响应定义

**成功响应（HTTP 200）**

| 字段编号 | 字段名 | 类型 | 必返 | 说明 |
|---|---|---|---|---|
| `IF-001-F10` | `token` | string | 是 | 会话令牌（JWT，HS256） |
| `IF-001-F11` | `expires_in` | integer | 是 | 有效期（秒），当前 7200 |
| `IF-001-F12` | `user.id` | string | 是 | 用户唯一标识 |
| `IF-001-F13` | `user.name` | string | 是 | 显示名 |
| `IF-001-F14` | `user.roles` | array[string] | 是 | 角色码列表，可为空数组 |

**失败响应（HTTP 4xx/5xx）**

| 字段编号 | 字段名 | 类型 | 必返 | 说明 |
|---|---|---|---|---|
| `IF-001-F20` | `code` | string | 是 | 业务错误码，见 §3.2.1.4 |
| `IF-001-F21` | `message` | string | 是 | 面向用户的可读消息（中文） |
| `IF-001-F22` | `trace_id` | string | 是 | 链路追踪 ID |
| `IF-001-F23` | `retry_after` | integer | 否 | 秒；仅在限流/锁定时返回 |

##### 3.2.1.4 错误码表

| 错误码 | HTTP | 含义 | 触发条件 | 调用方期望处置 | 是否可重试 |
|---|---|---|---|---|---|
| `AUTH-4001` | 400 | 入参格式错误 | 账号/口令长度或字符集不合规 | 修正入参，不重试 | 否 |
| `AUTH-4011` | 401 | 凭证无效 | 账号不存在**或**口令错误（**不区分**，防账号枚举） | 提示"账号或口令错误" | 否 |
| `AUTH-4012` | 401 | 令牌过期 | 会话超时 | 重新登录 | 否 |
| `AUTH-4231` | 423 | 账号锁定 | 连续 5 次失败，锁定 15min | 读 `retry_after`，等待 | 是（延时后） |
| `AUTH-4291` | 429 | 触发限流 | 单 IP > 100 次/分钟 | 读 `Retry-After` 退避 | 是（指数退避） |
| `AUTH-5001` | 503 | 认证服务不可用 | 下游超时/熔断 | 重试 1 次；仍失败则返回业务侧 503 | 是（≤ 1 次） |
| `AUTH-5002` | 500 | 服务内部错误 | 未预期异常 | 记录 `trace_id` 并报障 | 否 |

> **错误码规则**：错误码一经发布即为**对外契约**，不得改语义、不得复用。废弃错误码保留行并标注"已废弃"。

##### 3.2.1.5 幂等性

| 项 | 内容 |
|---|---|
| **是否幂等** | **否**（登录会刷新会话与失败计数） |
| **幂等键** | 不适用 |
| **重复调用影响** | 重复成功登录：生成新令牌，旧令牌仍有效至过期；重复失败：失败计数继续累加（**这是有意设计**，用于锁定策略） |
| **调用方约束** | 严禁对登录接口做**盲目自动重试**；仅在 `AUTH-5001`（服务不可用）时重试 1 次 |

##### 3.2.1.6 超时与重试

| 项 | 取值 | 说明 |
|---|---|---|
| 调用方连接超时 | 1s | |
| 调用方读超时 | 3s | **必须 > 服务方处理上限** |
| 服务方处理上限 | 2s | 由 `<外部系统 A>` 承诺 |
| 重试次数 | ≤ 1 | 仅对 `AUTH-5001` / 网络超时 |
| 重试间隔 | 500ms | 固定间隔（本接口无幂等键，不做指数退避堆积） |
| 熔断阈值 | 10s 内失败率 > 50% 且请求数 ≥ 20 → 熔断 30s | 熔断期间快速失败返回 `AUTH-5001` |
| 超限处置 | 超时即返回业务侧 `503`，**不得**无限等待 | |

##### 3.2.1.7 性能与容量要求

| 指标 | 要求 | 度量条件 |
|---|---|---|
| 响应时间 | P95 ≤ 500ms（含下游） | 50 并发，持续 10min |
| 吞吐 | ≥ 50 TPS | 同上 |
| 限流 | 单 IP 100 次/分钟；单账号 20 次/分钟 | 网关配置 |
| 可用性 | ≥ 99.9% | 月度统计 |

##### 3.2.1.8 版本兼容策略

| 项 | 内容 |
|---|---|
| 版本标识 | URL 路径版本 `/api/v1/` |
| **兼容扩展**（不升版本） | 新增**可选**请求字段；新增响应字段（消费方必须**忽略未知字段**）；新增错误码 |
| **破坏性变更**（必须升版本） | 删除/重命名字段、字段类型或语义变更、必填性由选填改必填、删除错误码 |
| 并行期 | 破坏性变更必须提供 **≥ 2 个发布周期**（且 ≥ 90 天）的旧版本并行期 |
| 弃用流程 | ① 文档标注 `Deprecated` + 弃用日期 → ② 响应头加 `Deprecation: true` 与 `Sunset` → ③ 并行期结束下线 |
| 消费方义务 | 必须容忍未知字段；必须处理新增错误码的兜底分支 |
| 变更记录 | 见 §3.2.1.9 |

##### 3.2.1.9 接口变更记录

| 版本 | 日期 | 变更类型 | 变更内容 | 兼容性 | 变更单号 | 影响消费方 | 已通知 |
|---|---|---|---|---|---|---|---|
| v1.0 | `<YYYY-MM-DD>` | 新建 | 初始定义 | — | — | — | — |
| v1.1 | `<YYYY-MM-DD>` | 兼容扩展 | 新增可选字段 `captcha_token`（`IF-001-F04`） | 向后兼容 | `FC-<YYYY>-<NNN>` | `<M20>` | ✅ |
| | | | | | | | |

> **变更类型**：新建 / 兼容扩展 / 破坏性变更 / 废弃。

---

#### 3.2.2 `<IF-007>` `<接口名称>`（异步接口填写示例骨架）

> 异步接口与同步接口的字段差异在「投递语义」「消费幂等」「死信处理」三项，其余字段相同。

| 项 | 内容 |
|---|---|
| **接口编号** | `<IF-007>` |
| **接口名称** | 任务状态变更通知 |
| **类型** | 外部接口（异步） |
| **方向** | 出（本系统发布，对端订阅） |
| **协议** | MQ（`<产品名>`）/ Topic `task.status.changed` |
| **数据格式** | JSON（消息体），Schema 见下 |
| **投递语义** | **至少一次（at-least-once）** —— 消费方必须幂等 |
| **分区键** | `task_id`（保证同一任务的状态变更有序） |
| **消息字段表** | 见下 |
| **幂等键** | `event_id`（UUID，全局唯一）；消费方须以 `event_id` 去重 |
| **重试与死信** | 消费失败自动重试 3 次（间隔 1s/5s/30s）；仍失败入死信队列 `task.status.changed.dlq`，告警并人工处置 |
| **积压告警** | 消费延迟 > 1000 条或 > 5min 触发告警 |
| **顺序保证** | 同一 `task_id` 内有序；跨 `task_id` 不保证 |
| **版本策略** | 消息体带 `schema_version`；兼容扩展不升主版本；破坏性变更需新旧双发并行期 ≥ 90 天 |

**消息字段表**

| 字段编号 | 字段名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `IF-007-F01` | `event_id` | string(uuid) | 是 | 事件唯一标识，幂等键 |
| `IF-007-F02` | `event_time` | string(ISO8601) | 是 | 事件发生时间（含时区） |
| `IF-007-F03` | `schema_version` | string | 是 | 如 `1.0` |
| `IF-007-F04` | `task_id` | string | 是 | 任务 ID（分区键） |
| `IF-007-F05` | `from_status` / `to_status` | string | 是 | 状态机前后态，取值见设计文档 |
| `IF-007-F06` | `operator` | string | 是 | 操作人标识；系统触发填 `system` |

**错误码表（消费方回执/nack 语义）**

| 错误码 | 含义 | 处置 |
|---|---|---|
| `MSG-4001` | 消息体反序列化失败 | 直接入死信，不重试 |
| `MSG-4091` | `event_id` 重复 | **视为成功**（ack），不重试 |
| `MSG-5001` | 下游依赖暂不可用 | nack，按退避策略重试 |

---

### 3.3 接口需求矩阵（接口 × 需求 × 模块）

| 接口编号 | 支撑需求 | 提供方模块 | 消费方模块 | 关联设计章节 | 关联测试用例 |
|---|---|---|---|---|---|
| `<IF-001>` | `<REQ-F-001>` | `<外部系统 A>` | `<M20>`、`<M60>` | HLD §3.3 | `TC-101`、`TC-201` |
| `<IF-002>` | `<REQ-F-002>` | `<外部系统 A>` | `<M20>`、`<M60>` | HLD §3.3 | `TC-102`、`TC-202` |
| `<IF-003>` | `<REQ-N-001>` | `<M40>` | `<M20>` | HLD §3.2 | `TC-003`、`TC-203` |
| `<IF-005>` | `<REQ-F-001>` | `<M60>` | `<M20>` | HLD §3.2 | `TC-010`、`TC-101` |
| `<IF-007>` | `<REQ-F-030>` | `<M20>` | `<外部系统 C>` | HLD §3.3 | `TC-104` |

---

## 4 合格性规定（接口如何被验证）

| 编号 | 验证项 | 方法 | 通过判据 | 阶段 |
|---|---|---|---|---|
| Q-01 | 契约一致性 | 契约测试（Schema 校验 / 双向 Mock） | 全部字段类型、必填性、错误码与本文档一致 | S3 骨架 / S5 集成 |
| Q-02 | 错误码覆盖 | 集成测试逐码注入 | 错误码表 100% 被用例覆盖 | S5 |
| Q-03 | 幂等性 | 重复提交同一请求 N 次 | 系统状态与提交 1 次一致（或按声明的非幂等语义一致） | S5 |
| Q-04 | 超时与重试 | 故障注入（延迟、断连） | 按声明策略重试；无无限等待；熔断生效 | S5 |
| Q-05 | 性能 | 压测 | 满足 §3.2.x.7 的指标 | S5 |
| Q-06 | 兼容性 | 旧版本客户端调新版本服务 | 兼容扩展下旧客户端功能正常 | S5 / S7 回归 |
| Q-07 | 安全 | 越权、重放、脱敏检查 | 100% 拦截；日志无明文敏感字段 | S5 / S6 |

---

## 5 接口追溯

| 方向 | 要求 | 校验方式 |
|---|---|---|
| 正向 | 每条接口需求（`IF-REQ-*`）与每个接口（`IF-*`）必须指向至少一条 SRS 需求 | 本文档 §3.3 与 RTM 的「接口编号」列 |
| 反向 | 每条涉及交互的 SRS 需求必须至少有一个接口或明确声明"无接口" | `tools/trace_matrix.py` 门禁 |

> **接口编号进入 RTM**：`03-需求追溯矩阵.csv` 的「接口编号」列填 `<IF-001>`（多个用 `;` 分隔；确无接口的填 `-`）。

---

## 6 附录

### 附录 A 待决事项（TBD）

| 编号 | 待决内容 | 影响的接口 | 责任人 | 计划关闭日期 | 状态 |
|---|---|---|---|---|---|
| `<TBD-01>` | 例如：`<外部系统 A>` 是否支持 `captcha_token`，待对方确认 | `<IF-001>` | `<姓名>` | `<YYYY-MM-DD>` | ⏳ 未关闭 |

### 附录 B 接口联调记录

| 日期 | 接口编号 | 对端 | 环境 | 结论 | 遗留问题 | 记录人 |
|---|---|---|---|---|---|---|
| `<YYYY-MM-DD>` | `<IF-001>` | `<外部系统 A>` | 联调环境 | 通过 | `<TBD-01>` | `<姓名>` |

---

## 7 填写指引

1. **最常见错误：只定义成功响应。** 每个接口的「错误码表」至少覆盖四类：入参错误（4xx）、权限/认证错误（401/403）、依赖故障（503）、限流（429）。**没有错误码表的接口 = 没定义完的接口。**
2. **最常见错误：把幂等性写成"应该幂等"。** 必须明确二选一：**是/否幂等**；非幂等接口必须给出幂等键字段或明确写出"禁止自动重试"的约束。含糊表述在集成期必然引发重复扣款/重复创建一类事故。
3. **最常见错误：超时值拍脑袋，且调用方超时 ≤ 服务方上限。** 必须满足 `调用方超时 > 服务方处理上限`，否则调用方先超时重试，服务方还在处理，直接产生重复请求。
4. **最常见错误：接口改了不通知消费方、不改 RTM。** 任何破坏性变更必须走 R5 变更评审，提供 ≥ 90 天并行期，并同步更新本文档 §3.2.x.9、§3.1 清单表与 `03-需求追溯矩阵.csv`。
5. **最常见错误：接口清单表与详细定义不同步。** 清单表是总账，详细定义是明细；新增接口**必须先登记编号**，废弃接口**必须保留行并标注替代接口**，编号永不复用。
6. **最常见错误：把内部接口排除在外。** 模块间内部接口同样要定义错误码与契约，其落地形态是 `../03-设计类/03-模块接口契约.md`；本文档与契约文件的接口编号必须一一对应。

---

## 8 依据说明

- 本文档的**章节骨架**（引言 / 编写目的 / 范围 / 定义 / 参考资料 / 需求 / 接口标识与优先级 / 功能与数据需求 / 性能需求 / 设计约束 / 合格性规定 / 需求可追溯性 / 注释 / 附录）按 **GB/T 8567-2006《计算机软件文档编制规范》** 对**接口需求规格说明**这一文档种类的要求组织；需求条目的可验证性要求参照 **GB/T 9385-2008**。
- **保守处理声明**：GB/T 8567-2006 中接口需求规格说明的精确分节标题、层级与条款编号，本模板未逐字复制，而是按通行工程结构（接口清单 + 逐接口详细定义 + 合格性规定 + 追溯）落地；§2 接口通则、§3.2 的字段/错误码/幂等/超时重试/版本兼容五张表、§4 合格性规定检查项，均为**本仓库的工程强化项**。**章节骨架按 GB/T 8567-2006 / GB/T 9385-2008 对接口需求规格说明文档种类的要求组织，实施时请以标准正文为准。**
- 标准状态核验日期：2026-02；使用前请到[国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/gb/)复核现行性。
