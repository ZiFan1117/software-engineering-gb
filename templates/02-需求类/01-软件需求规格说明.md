# 软件需求规格说明（SRS）

> **文档编号规则**：`<项目代号>-SRS-<顺序号>-v<主版本>.<次版本>`，示例 `<项目代号>-SRS-001-v1.0`（规则见 §0.2）
> **适用阶段**：**S1 需求工程**（准出门禁：**R1 需求评审**通过 + **需求基线** `req-baseline/v1.0` 已打标签）
> **依据标准**：**GB/T 9385-2008**《计算机软件需求规格说明规范》；**GB/T 8567-2006**《计算机软件文档编制规范》；过程依据 GB/T 8566-2022
> **文档状态**：**模板（Template）** · 未基线化 · 禁止未经填写与评审直接作为交付物提交

---

## 零、使用说明（填完全文后请删除本章）

### 0.1 填写顺序（建议）

```
① 文档信息与修订记录（§0.3 / §0.4）
   → ② 引言（第 1 章）
   → ③ 总体描述（第 2 章）：前景 / 功能概览 / 用户 / 约束 / 假设
   → ④ 可验证化改写（第 3 章）：把第 2 章的"愿望"逐条改写为可测指标
   → ⑤ 具体需求（第 4 章）：逐条卡片式填写功能与非功能需求
   → ⑥ 回填需求追溯矩阵（`03-需求追溯矩阵.csv`）
   → ⑦ 附录与索引（第 5、6 章）→ 提交 R1 需求评审
```

### 0.2 编号规则（全文强制统一）

| 对象 | 编号格式 | 示例 | 说明 |
|---|---|---|---|
| 本文档 | `<项目代号>-SRS-<顺序号>-v<主版本>.<次版本>` | `PRJ-SRS-001-v1.0` | 顺序号 3 位；版本随基线递增 |
| 功能需求 | `REQ-F-<3 位序号>` | `<REQ-F-001>` | 一经分配**永不复用**，删除后序号作废 |
| 非功能需求 | `REQ-N-<3 位序号>` | `<REQ-N-001>` | 性能、可靠性、安全、易用、可维护等 |
| 接口需求 | `IF-<3 位序号>` | `<IF-001>` | 详细定义见 `02-接口需求规格说明.md` |
| 测试用例 | `TC-<3 位数字>` | `TC-001` | 全局唯一；百位编码测试级别：`0xx` 单元 / `1xx` 集成 / `2xx` 系统 / `3xx` 验收（门禁强制，见 `03-需求追溯矩阵.说明.md` §2.0.1） |
| 设计模块 | `M<2 位序号>` | `<M01>` | 规则见 `../03-设计类/02-模块清单与模块号登记表.md` |
| 待决事项 | `TBD-<2 位序号>` | `<TBD-01>` | 必须写明责任人与关闭日期，否则不得通过 R1 |
| 日期 / 人名 | `<YYYY-MM-DD>` / `<姓名>` | `2026-02-14` / 张三 | 不使用相对时间（如"上周"） |

### 0.3 占位符约定

| 占位符 | 含义 | 是否允许留空 |
|---|---|---|
| `<项目名称>` | 项目全称（与合同/任务书一致） | 否 |
| `<项目代号>` | 文档编号用的短代号 | 否 |
| `<REQ-F-001>` / `<REQ-N-001>` | 功能 / 非功能需求编号 | 否 |
| `<IF-001>` | 接口编号 | 否 |
| `<M01>` | 设计模块号 | 否（未定则可填 `<TBD-01>`） |
| `<TBD-01>` | 待决事项 | 允许，但须有责任人与关闭日期 |
| `<YYYY-MM-DD>` / `<姓名>` | 日期 / 人名 | 否 |

### 0.4 裁剪说明

本模板按 GB/T 9385-2008 的章节骨架组织。项目可按规模裁剪**章节的深度**，但：

- **不得删除**：引言、总体描述、具体需求（外部接口/功能/性能/设计约束/软件系统属性/其他需求）、附录、索引 六大部分的**标题层级**；
- 裁剪必须在 `../01-策划类/02-项目开发计划.md` 的「裁剪说明」章节留痕（被裁剪项、理由、补偿措施、批准人）；
- **红线**：需求基线与需求追溯矩阵**不允许裁剪**。

### 0.5 文档信息

| 项 | 内容 |
|---|---|
| 项目名称 | `<项目名称>` |
| 文档编号 | `<项目代号>-SRS-001-v1.0` |
| 版本号 | v1.0 |
| 密级 | 内部 / 秘密（择一） |
| 编写人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 评审人 / 日期 | `<姓名>` / `<YYYY-MM-DD>`（R1 需求评审） |
| 批准人 / 日期 | `<姓名>` / `<YYYY-MM-DD>` |
| 需求基线标签 | `req-baseline/v1.0` |
| 关联文档 | 接口需求规格说明 `<项目代号>-IRS-001`；需求追溯矩阵 `<项目代号>-RTM-001` |

### 0.6 修订记录

| 版本 | 日期 | 修订人 | 修订类型 | 修订内容摘要 | 关联变更单 | 基线标签 |
|---|---|---|---|---|---|---|
| v0.1 | `<YYYY-MM-DD>` | `<姓名>` | 新建 | 初稿 | — | — |
| v1.0 | `<YYYY-MM-DD>` | `<姓名>` | 基线 | R1 评审后基线化 | — | `req-baseline/v1.0` |
| | | | | | | |

> 修订类型取值：新建 / 修改 / 基线 / 变更（变更类必须填关联变更单编号，如 `FC-2026-007`）。

---

## 1 引言

### 1.1 编写目的

说明本文档的编写目的、预期读者与用途：

- **目的**：定义 `<项目名称>` 的软件需求，作为设计、实现、测试、验收的**共同基准**；
- **预期读者**：甲方/产品、架构师、开发、测试、质量、配置管理员、验收方；
- **用途**：需求基线化后，任何需求变更须走变更控制流程，并同步更新需求追溯矩阵。

### 1.2 范围

| 项 | 内容 |
|---|---|
| 产品名称 | `<项目名称>` |
| 产品目标 | 用一句话说明产品要达成的业务目标 |
| 本文档覆盖范围 | 列出本文档覆盖的功能域/子系统（建议 3–7 条） |
| **明确不在范围内** | 列出**不做**的内容（这是最容易被跳过、也最有价值的一节） |
| 与其他系统的边界 | 列出上游/下游系统，并指向 `02-接口需求规格说明.md` 的对应 `<IF-001>` |

> **范围 ≠ 功能清单。** 范围的要点在"边界"：写清楚"不做什么"，比写"做什么"更能减少后期扯皮。

### 1.3 术语、定义与缩略语

| 术语 / 缩略语 | 全称 | 定义（本项目的约定含义） |
|---|---|---|
| SRS | Software Requirements Specification | 软件需求规格说明 |
| RTM | Requirements Traceability Matrix | 需求追溯矩阵 |
| P95 | 95th Percentile | 95 分位值，用于表达响应时间等指标的统计口径 |
| `<术语>` | | |

> **规则**：同一概念在全套文档中**只能有一个词**。出现同义词（如"用户/账号/操作员"混用）即为缺陷。

### 1.4 参考资料

| 编号 | 名称 | 编号/版本 | 来源 | 用途 |
|---|---|---|---|---|
| REF-01 | 项目任务书 / 合同 | `<编号>` | 甲方 | 需求来源 |
| REF-02 | GB/T 9385-2008 计算机软件需求规格说明规范 | — | 国家标准 | 本文档结构依据 |
| REF-03 | GB/T 8567-2006 计算机软件文档编制规范 | — | 国家标准 | 文档种类依据 |
| REF-04 | `<项目名称>` 项目开发计划 | `<项目代号>-PDP-001` | 本项目 | 裁剪说明、质量目标 |
| REF-05 | `<上游系统>` 接口文档 | `<编号>` | 第三方 | 外部接口需求来源 |

---

## 2 总体描述

### 2.1 产品前景

说明产品所处的业务/系统环境，以及它与既有系统的关系。

| 项 | 内容 |
|---|---|
| 产品定位 | 独立产品 / 某系统的子系统 / 既有产品的替换 |
| 上下文图 | 以文字或 ASCII 图给出：外部实体 → 本产品 → 外部实体 |
| 主要用户群体 | 角色清单（与 §2.3 一致） |
| 与既有系统的关系 | 替换 / 共存 / 集成，并说明数据迁移需求 |
| 运行环境概览 | 部署形态（单机 / 集群 / 云）、操作系统、浏览器等 |

```
        ┌──────────────┐        ┌──────────────────────┐        ┌──────────────┐
        │  <外部系统 A> │ ─────→ │                      │ ─────→ │  <外部系统 C> │
        └──────────────┘        │   <项目名称>          │        └──────────────┘
        ┌──────────────┐        │                      │        ┌──────────────┐
        │  <用户角色 B> │ ─────→ │                      │ ─────→ │  <数据存储 D> │
        └──────────────┘        └──────────────────────┘        └──────────────┘
```

### 2.2 产品功能（概览）

> 本节只给**功能概览**，用于让读者 5 分钟看懂产品；**逐条可测的需求在 §4.2**。

| 功能域编号 | 功能域名称 | 功能概述 | 覆盖需求编号 | 优先级 |
|---|---|---|---|---|
| FA-01 | 用户与权限 | 登录、注销、角色与权限管理 | `<REQ-F-001>`、`<REQ-F-002>` | P0 |
| FA-02 | `<功能域>` | | | P1 |

### 2.3 用户特征

| 角色 | 特征描述 | 使用频度 | 关键诉求 | 技能假设 |
|---|---|---|---|---|
| `<角色 A>` | 例如：业务管理员，日均操作 200 次 | 高 | 操作步数少、可批量 | 会使用浏览器 |
| `<角色 B>` | | | | |

### 2.4 约束

| 编号 | 约束类型 | 约束内容 | 来源 | 对需求的影响 |
|---|---|---|---|---|
| CON-01 | 法规/标准 | 例如：须符合 `<标准号>` | 合同 | |
| CON-02 | 技术 | 例如：必须运行于国产化操作系统 | 甲方 | |
| CON-03 | 进度 | 例如：<YYYY-MM-DD> 前完成 S5 系统测试 | 合同 | |
| CON-04 | 资源 | 例如：团队 4 人，无专职运维 | 内部 | |
| CON-05 | 接口 | 例如：必须复用既有统一认证 | 甲方 | |

### 2.5 假设与依赖

| 编号 | 类型 | 内容 | 若不成立的后果 | 验证时点 | 状态 |
|---|---|---|---|---|---|
| AS-01 | 假设 | 例如：上游数据源提供稳定的同步 HTTP 接口 | 需引入异步补偿，需求规模扩大 | S2 框架评审前 | ⏳ 待验证 |
| DP-01 | 依赖 | 例如：依赖甲方在 `<YYYY-MM-DD>` 前提供测试数据集 | 性能需求 `<REQ-N-001>` 无法验证 | S5 前 | ⏳ 待验证 |

> **规则**：每个假设与依赖都必须有**验证时点**与**责任人**。没有验证时点的假设 = 定时炸弹（与 `../01-流程与阶段/框架与模块共演化.md` 的框架假设清单同一原则）。

---

## 3 需求可验证化改写【本仓库定制章节】

> 本章是 GB/T 9385-2008 骨架之外的**工程强化项**，但它是本仓库的**硬要求**：
> S1 阶段必须把第 2 章里的"愿望式表述"逐条改写为可度量、可判定的表述，改写结果进入 §4。

### 3.1 反例 → 正例 对照表（必须逐条检查）

| ❌ 不可验证（禁止进入 SRS） | ✅ 可验证（改写后） | 判定方式 |
|---|---|---|
| 界面要美观 | 主流程操作步数 ≤ 5 步；关键操作响应 ≤ 200ms（P95） | 操作步数人工计数；响应时间由性能用例 `TC-210` 度量 |
| 系统要稳定 | 连续运行 72h 无未处理异常；MTBF ≥ 200h；P0 缺陷为 0 | 长稳用例 `TC-204`；缺陷清单统计 |
| 支持大数据量 | 单表 1000 万行下，列表查询响应 ≤ 2s（P95）；批量导入 10 万行 ≤ 5min | 性能用例 `TC-203`，指定数据集与统计口径 |
| 要安全 | 通过 OWASP Top 10 检查；口令存储 bcrypt(cost ≥ 10)；越权访问用例 100% 拦截 | 安全用例 `TC-230` + 扫描报告 |

### 3.2 改写硬规则

1. **可判定**：需求必须能给出"通过 / 不通过"的客观判定，不得依赖主观评价（"美观""友好""流畅"）。
2. **有指标**：凡涉及性能、容量、可靠性、安全的需求，必须给出**数值 + 统计口径 + 度量条件**（数据集规模、并发数、环境配置）。
3. **有边界**：必须说明异常路径与边界条件（空值、超限、超时、并发冲突、权限不足）。
4. **可追溯**：每条需求必须能指向至少一条测试用例编号（`TC-\d{3}` 格式），并回填到需求追溯矩阵（见 §4 卡片的「关联测试用例」字段）。
5. **⭐ 总规则**：**任何写进 SRS 的需求，都必须能推导出至少一条测试用例。写不出测试用例的需求 = 不合格需求，不得基线化。**

> **R1 需求评审的判定口径**：评审时逐条问"这条需求，测试怎么判它过了？"
> 答不上来的需求，一律退回改写，不接受"先基线化、后面再细化"。

### 3.3 自查表

| 检查项 | 通过判据 | 结论 |
|---|---|---|
| 是否含主观形容词（美观/友好/高效/尽量/等） | 全文检索为 0 | ☐ |
| 非功能需求是否全部量化 | 100% 含量化指标 | ☐ |
| 每条需求是否有验收准则 | 100% 有 | ☐ |
| 每条需求是否可推导出测试用例 | 100% 可 | ☐ |
| 是否有未闭环 TBD | 0（或均有责任人与关闭日期） | ☐ |

---

## 4 具体需求

### 4.1 外部接口需求

> 本节只做**索引与约束**，接口的完整定义（字段表、错误码、幂等性、超时重试、版本兼容）写在
> `02-接口需求规格说明.md`，本表通过 `<IF-001>` 引用。

| 接口编号 | 接口名称 | 类型 | 对端系统/模块 | 协议 | 数据格式 | 频率/吞吐 | 关联需求 |
|---|---|---|---|---|---|---|---|
| `<IF-001>` | 统一认证登录 | 同步 | `<外部系统 A>` | HTTPS/REST | JSON | 峰值 50 TPS | `<REQ-F-001>` |
| `<IF-002>` | `<接口名称>` | 异步 | `<M20>` | MQ | JSON | | |

| 需求编号 | 需求描述 |
|---|---|
| IF-REQ-01 | 所有外部接口必须定义**错误码表**与**幂等性**约定 |
| IF-REQ-02 | 所有外部接口必须有**超时**与**重试**策略，重试必须幂等 |
| IF-REQ-03 | 接口必须声明**版本兼容策略**（向后兼容 / 破坏性变更需并行期） |

### 4.2 功能需求（卡片式，逐条编号）

> **填写纪律：一条需求一张卡。** 禁止把多个需求塞进一张卡，也禁止把一条需求拆成多张卡。
> 「关联设计模块」填 `<M01>` 形式的模块号（S2 未完成时可先填 `<TBD-01>`，但必须在 R1 前闭环到模块号或明确标记为设计阶段决策项）。

#### 4.2.1 卡片字段说明

| 字段 | 必填 | 说明 |
|---|---|---|
| 需求编号 | ✅ | `REQ-F-xxx`，唯一，永不复用 |
| 名称 | ✅ | 动宾短语，≤ 12 字，可检索 |
| 优先级 | ✅ | P0（必须）/ P1（应该）/ P2（可选），见 §4.2.4 |
| 来源 | ✅ | 合同条款号 / 会议纪要 / 用户访谈记录编号 / 法规条款 |
| 描述 | ✅ | "系统应当……"，一句话说清行为，禁止主观形容词 |
| 输入 / 处理 / 输出 | ✅ | 各自的来源、转换规则、去向；处理规则要写到可编码的程度 |
| 异常与边界 | ✅ | 至少覆盖：空值、超限、超时、并发、权限不足、外部依赖失败 |
| 验收准则（可测） | ✅ | Given/When/Then 风格，每条准则可映射到用例 |
| 关联设计模块 | ✅ | `<M01>` 形式 |
| 关联测试用例 | ✅ | 至少 1 条，格式必须为 `TC-\d{3}`（如 `TC-001` / `TC-101` / `TC-201` / `TC-301`），并与 `03-需求追溯矩阵.csv` 的四个用例列一致 |

#### 4.2.2 示例一：功能需求

---

**`<REQ-F-001>` 用户登录**

| 字段 | 内容 |
|---|---|
| **需求编号** | `<REQ-F-001>` |
| **名称** | 用户登录（账号口令认证） |
| **优先级** | P0 |
| **来源** | 合同附件一 §3.1；用户访谈记录 `<YYYY-MM-DD>`-02 |
| **描述** | 系统应支持已注册用户以"账号 + 口令"方式登录；认证成功后建立会话并跳转至其默认首页。同一账号连续 5 次认证失败，锁定 15 分钟。 |
| **关联设计模块** | `<M20>`（业务层-认证服务）；依赖 `<M60>`（基础设施-统一认证适配） |
| **关联接口** | `<IF-001>`（统一认证登录，同步 / HTTPS / JSON） |
| **关联测试用例** | `TC-001`（认证成功/失败）、`TC-002`（锁定策略）、`TC-101`、`TC-201`、`TC-301` |

**输入 / 处理 / 输出**

| 项 | 内容 |
|---|---|
| **输入** | `account`：字符串，6–32 位，字母数字下划线；`password`：字符串，8–64 位；来源：登录页表单（POST `/api/v1/auth/login`） |
| **处理** | ① 校验入参格式，不合法直接返回 400；② 查询 `<M40>` 获取用户记录，取 `password_hash`；③ 以 bcrypt(cost≥10) 校验口令；④ 失败则 `fail_count += 1`，达 5 次写锁定标记（TTL 15min）；⑤ 成功则清零 `fail_count`，生成会话令牌（有效期 2h，滑动续期） |
| **输出** | 成功：`200` + `{token, expires_in, user:{id, name, roles[]}}`；失败：`401` + 错误码；锁定：`423` |

**异常与边界**

| 场景 | 期望行为 |
|---|---|
| 账号不存在 | 返回 `401` 且错误码与"口令错误"**一致**（不泄露账号存在性） |
| 口令为空 / 超长（> 64） | 返回 `400`，不写入失败计数 |
| 连续失败第 5 次 | 第 5 次即锁定，返回 `423`，并记录审计日志 |
| 锁定期间再次尝试 | 返回 `423`，附带 `retry_after` 秒数 |
| 认证服务（`<IF-001>`）超时（> 3s） | 重试 1 次（幂等：仅查询不写状态）；仍失败返回 `503`，不锁定账号 |
| 并发登录同一账号 | 允许，会话互不影响；锁定计数以原子操作累加 |
| 数据库不可用 | 返回 `503`，记录 ERROR 日志，不返回 500 堆栈 |

**验收准则（可测）**

| 编号 | 准则（Given / When / Then） | 对应用例 |
|---|---|---|
| AC-01 | Given 已注册用户，When 提交正确账号口令，Then 200 且返回有效 token | `TC-001` |
| AC-02 | Given 已注册用户，When 提交错误口令，Then 401 且响应体不含账号是否存在的信息 | `TC-001` |
| AC-03 | Given 已连续失败 4 次，When 第 5 次失败，Then 423 且在 15 分钟内正确口令也被拒绝 | `TC-002` |
| AC-04 | Given 认证服务不可用，When 登录，Then 503 且账号失败计数不变 | `TC-101` |
| AC-05 | Given 任意一次登录请求，When 记录日志，Then 日志中口令字段为掩码且不可还原 | `TC-201` |

---

#### 4.2.3 示例二：非功能需求

---

**`<REQ-N-001>` 列表查询性能**

| 字段 | 内容 |
|---|---|
| **需求编号** | `<REQ-N-001>` |
| **名称** | 任务列表查询响应时间 |
| **优先级** | P0 |
| **来源** | 合同附件一 §5.2 性能要求；甲方性能测试大纲 |
| **描述** | 在 §4.2.3.1 规定的度量条件下，任务列表查询接口的响应时间 P95 ≤ 2s、P99 ≤ 5s，错误率 < 0.1%。 |
| **关联设计模块** | `<M40>`（数据访问-查询仓储）；消费方 `<M20>` |
| **关联接口** | `<IF-010>`（列表查询，同步 / HTTPS / JSON） |
| **关联测试用例** | `TC-003`（查询执行计划断言）、`TC-103`、`TC-203`（性能）、`TC-303` |

**度量条件（缺一不可，否则需求不可验证）**

| 项 | 取值 |
|---|---|
| 数据集规模 | 单表 1000 万行，任务归属 1000 个租户，分布均匀 |
| 并发 | 50 并发用户，持续 10 分钟 |
| 环境 | 与生产同规格（4C8G × 2 应用 + 8C32G 数据库），数据与生产同构 |
| 统计口径 | 服务端处理时间（不含网络），P95/P99 由压测工具直方图统计 |
| 判定样本 | 有效请求 ≥ 30000 次 |

**输入 / 处理 / 输出**

| 项 | 内容 |
|---|---|
| **输入** | `tenant_id`（必填）、`status`（可选）、`keyword`（可选）、`page`/`page_size`（默认 1/20，上限 100） |
| **处理** | ① 租户隔离条件强制注入；② 关键字走索引前缀匹配；③ 分页使用游标（避免深分页全表扫描）；④ 结果缓存 60s（按租户 + 查询条件哈希） |
| **输出** | `{items:[...], next_cursor, total_estimate}` |

**异常与边界**

| 场景 | 期望行为 |
|---|---|
| `page_size` > 100 | 截断为 100，返回 `X-Truncated: true`，不报错 |
| 游标非法 / 过期 | 返回 `400` + `CURSOR_INVALID`，提示重新查询 |
| 关键字为空字符串 | 视为未传，不退化为全表 LIKE '%%' |
| 单租户数据 > 500 万行 | 仍需满足 P95 ≤ 2s；若实测不达标，触发框架变更 `FC-1` 并重新设计查询路径 |
| 数据库慢查询（> 5s） | 熔断并返回 `503` + `retry_after`，记录慢查询日志 |

**验收准则（可测）**

| 编号 | 准则 | 对应用例 |
|---|---|---|
| AC-01 | Given 度量条件全部满足，When 执行 10 分钟压测，Then P95 ≤ 2s 且 P99 ≤ 5s 且错误率 < 0.1% | `TC-203` |
| AC-02 | Given 任意一次查询，When 检查执行计划，Then 命中索引且不使用全表扫描 | `TC-003` |
| AC-03 | Given `page_size=1000`，When 查询，Then 返回 ≤ 100 条且带截断标记 | `TC-204` |
| AC-04 | Given 两个租户并发查询，When 比对结果，Then 数据互不越界（100%） | `TC-205` |

---

#### 4.2.4 优先级定义

| 优先级 | 含义 | 若无法实现 | 门禁影响 |
|---|---|---|---|
| **P0** | 必须实现，缺失则产品不可用 | 项目不可交付 | 未实现不得通过 R6/R7 |
| **P1** | 应该实现，影响核心体验 | 需书面批准并记入遗留清单 | 需甲方/产品书面同意 |
| **P2** | 可选/增强 | 可延期至后续版本 | 记入版本说明 |

#### 4.2.5 需求填写区（按卡片逐条复制）

> 按 §4.2.2 / §4.2.3 的格式逐条填写，编号连续且不跳号。

---

**`<REQ-F-002>` `<需求名称>`**

| 字段 | 内容 |
|---|---|
| **需求编号** | `<REQ-F-002>` |
| **名称** | |
| **优先级** | P0 / P1 / P2 |
| **来源** | |
| **描述** | |
| **关联设计模块** | `<M01>` |
| **关联接口** | `<IF-001>` / 无 |
| **关联测试用例** | |

| 项 | 内容 |
|---|---|
| **输入** | |
| **处理** | |
| **输出** | |

| 异常与边界 | 期望行为 |
|---|---|
| | |

| 编号 | 验收准则（Given / When / Then） | 对应用例 |
|---|---|---|
| AC-01 | | |

---

### 4.3 性能需求

| 需求编号 | 性能项 | 指标 | 度量条件 | 关联需求 | 关联用例 |
|---|---|---|---|---|---|
| `<REQ-N-001>` | 列表查询响应时间 | P95 ≤ 2s，P99 ≤ 5s | 见 §4.2.3 | `<REQ-F-010>` | `TC-203` |
| `<REQ-N-002>` | 并发用户数 | ≥ 200 并发，错误率 < 0.1% | 与生产同构环境 | — | `TC-210` |
| `<REQ-N-003>` | 批量导入吞吐 | 10 万行 ≤ 5min | 单文件 ≤ 200MB | `<REQ-F-020>` | `TC-213` |
| `<REQ-N-004>` | 资源占用 | 应用常驻内存 ≤ 2GB，CPU 峰值 ≤ 80% | 200 并发下 | — | `TC-211` |

### 4.4 设计约束

| 编号 | 约束内容 | 理由 | 影响的设计决策 |
|---|---|---|---|
| DC-01 | 必须使用 `<技术栈>`，不得引入 `<某类依赖>` | 甲方运维能力 | 框架选型范围 |
| DC-02 | 数据不得出内网，禁止调用公网 API | 合规 | 外部接口 `<IF-001>` 需内网代理 |
| DC-03 | 单次部署包 ≤ `<N>` MB | 分发渠道限制 | 依赖裁剪 |
| DC-04 | 必须提供无障碍（键盘可达）支持 | 合规 | 前端组件选型 |

### 4.5 软件系统属性

> 按 GB/T 25000.51-2016 的质量特性组织，便于 S6 验收测试直接映射。

| 质量特性 | 子特性 | 需求编号 | 量化要求 | 验收映射 |
|---|---|---|---|---|
| 功能性 | 功能完备性、正确性 | `<REQ-F-001>`… | 需求覆盖率 100% | `TC-301`… |
| 性能效率 | 时间特性、资源利用率 | `<REQ-N-001>`… | 见 §4.3 | `TC-210`… |
| 兼容性 | 共存性、互操作性 | `<REQ-N-010>` | 支持浏览器 `<N>` 及以后 2 个大版本 | `TC-220` |
| 易用性 | 易学性、易操作性 | `<REQ-N-011>` | 主流程操作步数 ≤ 5 步；新用户 10min 内完成首个任务 | `TC-221` |
| 可靠性 | 成熟性、容错性、易恢复性 | `<REQ-N-012>` | 连续 72h 无未处理异常；MTBF ≥ 200h；故障恢复 ≤ 5min | `TC-204` |
| 信息安全性 | 保密性、完整性、抗抵赖 | `<REQ-N-013>` | 口令 bcrypt(cost≥10)；越权拦截 100%；关键操作留审计日志 | `TC-230`… |
| 维护性 | 模块化、可重用性、易分析性 | `<REQ-N-014>` | 模块间无循环依赖；单元测试行覆盖率 ≥ 80%，核心分支 ≥ 90% | CI 门禁 |
| 可移植性 | 适应性、易安装性 | `<REQ-N-015>` | 一条命令部署；支持 `<OS 列表>` | `TC-240` |

### 4.6 其他需求

| 编号 | 类别 | 需求内容 | 验收方式 |
|---|---|---|---|
| OR-01 | 数据保留 | 业务数据保留 ≥ 5 年，日志保留 ≥ 180 天 | 配置核查 |
| OR-02 | 备份恢复 | 每日全量备份，RPO ≤ 24h，RTO ≤ 4h | 恢复演练记录 |
| OR-03 | 审计 | 关键操作（登录、授权变更、数据删除）100% 留痕，含操作人/时间/IP | 日志抽样 |
| OR-04 | 国际化 | 界面文案与代码分离，支持中/英 | 切换验证 |
| OR-05 | 合规 | 符合 `<法规/标准号>` | 合规检查表 |

---

## 5 附录

### 附录 A 待决事项清单（TBD）

> **R1 门禁**：TBD 可以为非空，但**每条必须有责任人与关闭日期**，否则不得基线化。

| 编号 | 待决内容 | 影响的需求 | 责任人 | 计划关闭日期 | 状态 |
|---|---|---|---|---|---|
| `<TBD-01>` | 例如：登录锁定阈值取 5 次还是 3 次，待甲方确认 | `<REQ-F-001>` | `<姓名>` | `<YYYY-MM-DD>` | ⏳ 未关闭 |
| `<TBD-02>` | | | | | |

### 附录 B 需求来源索引

| 来源编号 | 来源类型 | 名称/位置 | 获取日期 | 获取人 | 涉及需求 |
|---|---|---|---|---|---|
| SRC-01 | 合同 | 附件一 §3.1 | `<YYYY-MM-DD>` | `<姓名>` | `<REQ-F-001>` |
| SRC-02 | 访谈 | 访谈记录 2026-02-10-02 | `<YYYY-MM-DD>` | `<姓名>` | |

### 附录 C 需求变更记录

| 变更单号 | 日期 | 变更类型 | 变更内容 | 影响需求 | 影响设计模块 | 是否已同步 RTM |
|---|---|---|---|---|---|---|
| `FC-<YYYY>-<NNN>` | `<YYYY-MM-DD>` | 新增/修改/删除 | | `<REQ-F-001>` | `<M01>` | ☐ |

### 附录 D 术语表（备用扩展）

---

## 6 索引

### 6.1 需求编号索引

| 需求编号 | 名称 | 章节 | 优先级 |
|---|---|---|---|
| `<REQ-F-001>` | 用户登录 | §4.2.2 | P0 |
| `<REQ-F-002>` | | §4.2.5 | |
| `<REQ-N-001>` | 列表查询性能 | §4.2.3 | P0 |

### 6.2 关键词索引

| 关键词 | 出现章节 |
|---|---|
| 登录 | §4.2.2、§4.5 |
| 性能 | §4.3、§4.5 |

### 6.3 接口索引

| 接口编号 | 名称 | 对端 | 详见 |
|---|---|---|---|
| `<IF-001>` | 统一认证登录 | `<外部系统 A>` | `02-接口需求规格说明.md` §3.2 |

---

## 7 填写指引

1. **别把 SRS 写成需求收集箱。** SRS 是**基线**，不是"想到什么先记下来"。未确认的信息放进附录 A 的 TBD，不要混进 §4 的需求卡片里充当正式需求。
2. **最常见错误：需求写成愿望。** "美观""稳定""友好""高效""尽量""等"这类词全文检索必须为 0。每一句都要能回答"测试怎么判它过了？"（见 §3）。
3. **最常见错误：只写成功路径。** 每条需求卡片的「异常与边界」至少要覆盖空值、超限、超时、并发、权限不足、外部依赖失败六类中的相关项。只写 happy path 的需求，到 S5 会集中爆炸。
4. **最常见错误：非功能需求不量化。** "响应要快"不合格；"P95 ≤ 2s，在 1000 万行 / 50 并发下度量"才合格。**没有度量条件的性能指标等于没有指标。**
5. **最常见错误：编号跳号或复用。** 需求编号一经分配**永不复用**（删除后序号作废），否则历史追溯与 RTM 会指向错误的条目。
6. **最常见错误：改了 SRS 不改 RTM。** 需求基线的任何变更，必须同步更新 `03-需求追溯矩阵.csv`，否则 CI 门禁 `tools/trace_matrix.py` 会失败，且追溯链断裂。

---

## 8 依据说明

- 本文档的**章节骨架**（引言 / 编写目的 / 范围 / 定义 / 参考资料 / 总体描述 / 产品前景 / 产品功能 / 用户特征 / 约束 / 假设与依赖 / 具体需求 / 外部接口需求 / 功能需求 / 性能需求 / 设计约束 / 软件系统属性 / 其他需求 / 附录 / 索引）按 **GB/T 9385-2008《计算机软件需求规格说明规范》** 对软件需求规格说明文档种类的要求组织；文档**种类与编号体系**参照 **GB/T 8567-2006《计算机软件文档编制规范》**。
- **保守处理声明**：上述标准正文的分节层级、章节编号与措辞，本模板未逐字复制（国标正文有版权，且本仓库不复制标准正文）。§3「需求可验证化改写」、§4.2 卡片式功能需求字段集、§4.5 与 GB/T 25000.51-2016 质量特性的映射表，均为**本仓库的工程强化项**，不是标准原文要求。**章节骨架按 GB/T 8567-2006 / GB/T 9385-2008 对软件需求规格说明文档种类的要求组织，实施时请以标准正文为准。**
- 标准状态核验日期：2026-02；使用前请到[国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/gb/)复核现行性。
