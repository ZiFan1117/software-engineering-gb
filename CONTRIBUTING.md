# 贡献指南

> 本仓库自己就是「按国标流程开发」的示范。所以对仓库的任何改动，**都要走同一套流程**。

---

## 一、本地跑通门禁（提交前必做）

```powershell
# 1) S3 门禁：骨架是否跑得起来（L0 冒烟）
python skeleton/run_skeleton.py

# 2) S4 门禁：单元测试（L1）
python tests/test_module_system.py          # 零依赖
python -m pytest tests -q                    # 装了 pytest 时（功能更全）

# 3) S1/S5/S6 门禁：需求双向追溯完整性
python tools/trace_matrix.py

# 4) 受控变更门禁：改动是否越界
python tools/scope_check.py --base main --scope .scope-declaration.json
```

**四项全绿才算"本地门禁通过"。** 门禁不通过就提交，等于绕过了整套流程。

---

## 二、改动前先判断：这是普通变更还是框架变更？

| 改动内容 | 变更性质 | 需要什么 |
|---|---|---|
| 修 bug、加测试、改文档措辞 | 普通变更 | PR + 检查单 |
| 新增模块（不改既有契约） | 普通变更 | PR + 更新模块登记表 + RTM |
| **改模块号规则 / 分层规则** | **框架变更** | **R5 变更评审 + 影响分析** |
| **改 `skeleton/module_system.py`** | **框架变更** | **R5 变更评审 + 影响分析** |
| **改 `tools/` 或 CI 门禁规则** | **框架变更** | **R5 变更评审 + 影响分析** |
| **改模块契约（`provides` / `depends_on`）** | **框架变更** | **R5 变更评审 + 影响分析** |

框架变更必须回答六个问题（对应 `docs/01-流程与阶段/框架与模块共演化.md`）：

1. 触发条件是 FC-1…FC-6 中的哪一条？（**FC-6 个人偏好一律驳回**）
2. 现象证据是什么？（不能只说"感觉不合适"）
3. 受影响模块有哪些？—— 用 `registry.impact_of([...])` 机器推导，不要靠回忆
4. 受影响需求有哪些？—— 反查 RTM
5. 需重跑哪些测试？—— 由影响分析推导回归策略（R-A…R-D）
6. 方案对比：至少 2 案，含"不改"或"最小改"

---

## 三、提交规范（Conventional Commits）

```
<类型>(<范围>): <简述>

[正文：为什么改，而不是改了什么]

追溯：REQ-F-012 / M03 / BUG-021 / FC-2026-007
AI 辅助：是/否（若为是，说明人工验证方式）
```

类型：`feat` `fix` `framework` `refactor` `docs` `test` `chore`，不兼容变更用脚注 `BREAKING CHANGE:`。

`framework` 类型是本仓库的强化项——让框架变更在提交历史里一眼可辨，便于统计
「框架变更次数 / 已实现模块数」这个健康度指标。

---

## 四、新增一个模块的标准动作

```powershell
# 1) 分配模块号（按分层分段，永不复用）
#    M01–M19 表现层 | M20–M39 业务层 | M40–M59 数据访问层
#    M60–M79 基础设施层 | M80–M99 公共/工具层

# 2) 在 skeleton/modules.py 中登记 ModuleDef
#    必填：module_id / name / version / responsibility / not_responsible / depends_on / provides
#    注意：not_responsible（不负责什么）是必填项——职责边界不清是模块划分失败的头号原因

# 3) 同步更新 templates/03-设计类/02-模块清单与模块号登记表.md

# 4) 补单元测试（正常 / 边界 / 异常 / 空值 四类路径）

# 5) 更新 RTM：把需求映射到新模块

# 6) 跑门禁：契约校验会检查模块号格式、依赖完整性、分层方向、循环依赖
python skeleton/run_skeleton.py
```

---

## 五、绝不接受的做法

| 做法 | 为什么 |
|---|---|
| 跳过门禁直接合并 | 门禁是唯一的质量控制点 |
| 改了代码不改设计文档 | 文档漂移 = 追溯体系归零 |
| 静默改框架（不写变更申请） | 其他人基于旧契约开发，集成时爆炸 |
| 为了让门禁通过而降低断言强度 | 形式化通过比不通过更危险 |
| 提交密钥或凭据 | 安全事故；一旦误提交必须立即吊销轮换 |
| 提交自己看不懂的 AI 生成代码 | 见 `docs/05-AI协作/AI协作规范.md` |
| 用 AI 编造实测数据或评审结论 | AI 可代写，不可代签 |

---

## 六、文档与模板的修改

- 修改 `docs/` 下的规范类文档，等同于**修改流程本身**，需要在 PR 中说明变更理由与影响；
- 修改 `templates/` 下的模板，需同步检查 `docs/00-标准依据/国标依据与标准矩阵.md` 的
  「阶段 × 交付物 × 标准依据」矩阵是否仍准确；
- 引用国标时，**必须写明标准号与年份**，并避免引用已被替代的老标准
  （典型坑：软件测试应引 GB/T 38634 系列，而非 GB/T 15532-2008）。
